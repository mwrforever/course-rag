# 开发环境配置
# 激活方式: --spring.profiles.active=dev

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.commerce.rag: DEBUG
    org.springframework.ai: DEBUG
