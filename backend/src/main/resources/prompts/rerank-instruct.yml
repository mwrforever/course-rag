# Rerank 指令 —— 注入 qwen3-rerank 的 instruct 字段
# 使用方式：通过 DashScopeRerankModel 覆写注入

rerank:
  instruct: |
    判断以下文档片段与查询问题的相关性。重点关注：
    1. 片段是否包含问题的直接答案
    2. 片段是否涉及相同主题或领域
    3. 片段中的关键实体是否与查询匹配

    请为每个片段打分（0~1），1 表示完全相关，0 表示完全无关。
    优先考虑包含具体操作步骤、代码示例或数据指标的片段。
