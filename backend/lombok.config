# BUG-9 修复：@RequiredArgsConstructor 默认不复制字段注解到生成构造器参数——
# @Qualifier 留在 final 字段上时 Spring 多 Bean 注入只能靠参数名匹配（改名即错配且无编译期提示）。
# copyableAnnotations 使 @Qualifier 随 Lombok 生成的构造器参数携带，注入契约显式化。
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
