# CHANGELOG.md — 工程变更记录

> 追加式记录：每次修订先在此追加（日期 · 变更简述 · 原因），再改正文。

---

- 2026-08-29 · 对话流式链式时间轴改版（feature/2026-08-28-chat-streaming-timeline，三批 SDD 交付）：①SSE 内容节点事件补全（后端）——thinking 载荷补 `{stage}` 思考来源阶段键（understanding/attachments/generating）、thinking_end 补 `{stage}` 配对、新增 QUERY_PLAN 事件（意图 code + 改写查询列表 + 课程名过滤，前端 zod 边界校验），seq 1-based 全链一致（SSE id 行/落库/回放）；②前端时间轴模型（Task 5-7）——StreamMessage.timeline 判别联合五节点（stage/queryPlan{intent,rewritten,courseNames}/thinking{stage,lines,ended}/sources/tool）按 SSE 到达序 push/merge：thinking 同 stage 合并行、tool_result 按 toolCallId 原位配对、query_plan/sources 二推原位替换幂等、delta 正文不入轴；历史回放（R1）按 (createdAt,seq) 稳定排序经 history-adapter 同构重建时间轴（thinking 行 thinkingStage 列直通，存量 null 降级 generating）；③链式时间轴组件族（Task 8-11，问渠主题令牌适配）——ChainTimeline 一根渐变竖线串联 OpStep（阶段/检索/工具步骤：运行态 shimmer+跳动点、完成态摘要+可展开 JSON 详情）/ThinkingStep（默认收起锚定底部露最新行、Brain→绿勾）/QueryPlanStep（意图标签+改写清单+过滤胶囊）；答案区 Markdown 映射覆写（h2/h3/li/strong 语义类）+左渐变竖线+光标；召回抽屉 chunk 卡（类型徽标+相似度 meter+点击展开全文+错峰进场）；④输入区扩容（Task 12）——附件 chips 内嵌输入卡内顶部 border-t 分隔（图一形态）、格式图标映射（ImageSquare/FilePdf/FileDoc/FileXls/FilePpt/FileText）、chip 点击弹窗预览（react-medium-image-zoom 图片放大/pdf iframe/其他图标卡+下载）、整页拖拽上传+高亮层；⑤会话弹窗化（Task 13）——行内改名→RenameDialog（预填+zod 非空≤50+Enter）、搜索框→聚焦浮层面板（防抖 keyword+max-h-72+空态+Esc/外点关）、新建对话按钮信号化（/chat 同路由经 Context 信号 reset 干净态不重挂载：清消息/附件/输入+会话归属）、删工作区顶栏「新建对话」入口（收敛侧栏）；⑥渲染性能（Task 14）——消息行/MarkdownView/ChainTimeline/ThinkingStep/QueryPlanStep memo 化+回调 useCallback：50 条历史+141 帧流式实测帧率 20.6→59.8fps（+190%）、流式窗口耗时 -71%（量化对比落盘 docs/progress/2026-08-29-时间轴渲染性能量化对比.md）；⑦E2E/文档收尾（Task 15a）——sse-events 补 query_plan/stage-thinking 用例并对齐时间轴 testid、新增 chat-timeline.spec（渲染序/折叠/抽屉）、attachments/sidebar spec 对齐扩容与弹窗、设计文档 §1.5.4 事件表补 stage/query_plan/thinking.stage 行。测试：后端 verify 全绿（时间轴批新增 stream/worker/service 单测）、C 端 456 单测绿（statements 90.63%）+ E2E 全绿（workers=1 web-first）；执行方式 SDD 三批派遣+批间独立审核。原因：用户任务指令——内容节点事件补全（思考实时流/查询计划展示/检索与系统 chunk 原位呈现）、工具调用并入时间轴、上传扩容+预览弹窗、会话管理弹窗化、新建干净会话、流式流畅性量化优化。
- 2026-08-27 · B 端教师管理端全量 UI 重构（Edukors 紫系，feature/2026-08-27-b-admin-ui-rebrand）：以 `code (5)/(14).html` 两份设计稿为唯一视觉基准，纯前端重构（后端与 `lib/api.ts`/`lib/types.ts` 契约冻结）：①设计系统——@theme 令牌名不动只换值（brand #6C5AE0 族/背景 #EFEEF9/紫调柔影/弹簧曲线），接入 @fontsource/plus-jakarta-sans 5.3.0（latin 400~800，Geist Sans 随之移除），新增 PageHead/StatCard/DataTable/DropdownMenu/EmptyState/ConfirmDialog 共享组件与 v-reveal 指令/useCountUp composable（新组件行覆盖 93.75%~100%）；②布局壳——白色 256px/折叠 80px 侧栏（边缘圆钮/激活指示条弹簧位移/分组手风琴 max-height/折叠 tooltip/<900px 抽屉）+ sticky 毛玻璃顶栏，navGroups/角色过滤纯函数原样复用；③登录页——四 SVG 小怪引擎（idle 跟随/typing 探头/peek 偷看/眨眼，rAF+lerp 纯函数单测）+ 右栏轨道视差 + 浮动标签表单 + Caps Lock + 错误 shake + 成功圆环 overlay，认证流与校验契约不变（认证路径 100% 覆盖保持）；④仪表盘——KPI 四卡 count-up（无环比箭头，负向契约）+ CSS/SVG 自绘图表（趋势柱状 7/30 天切换/意图 donut/赞踩堆叠条），echarts/vue-echarts 依赖移除（全仓零引用）；⑤13+ 业务视图全部按设计稿重制（知识库四视图/课程七文件/学生教师反馈/审计三视图/403/404），script 业务逻辑零改动，testid 契约（quick-*/save-basic/confirm-delete/etl-badge-toggle 等）保留；设计稿无后端支撑的装饰业务（购物车/货币/语言/通知/Revenue/统计假数/demo 账号等）全部丢弃不引入。执行方式：graph 节点门禁制（N0~N11）+ 调研/实现/审核 subagent 派遣 + N3~N8 八 worktree 并行（N5/N8a 的 FeedbackView 交叠经 N9 整合裁决）。测试：B 端 40 文件 394 单测全绿（全局行覆盖 90.03%）+ 8 spec 24 E2E 全绿（workers=1）+ lint/typecheck/prettier 全过；关键页 10 屏截图视觉核对落盘 docs/progress/。原因：用户任务指令——按两份 Edukors 设计稿完整重构教师管理端样式/动画/交互，后端未涉及的模块不新增、只在现有业务功能上优化。
- 2026-08-27 · C 端对话流式体验改版 + 单层导航/门控/首页动效/课程中心筛选（feature/2026-08-27-c-chat-stage-streaming-ui）：①检索链路响应感知修复（后端）——SSE 新增 STAGE 阶段事件（SseEventType.STAGE + SseEventTransformer.transformStages 按 SAA 非流式节点完成 chunk 映射阶段跃迁：QU 完成→检索/生成、retrieveNode 完成→生成、reactAgent 首模型 chunk→生成兜底，RunState CAS 去重），METADATA 前移到附件处理前（原位于附件管线最长 60s 静默窗口之后），附件处理前推 STAGE(attachments)/图执行前推 STAGE(understanding)——「先准备上下文，再流式返回」全程可见；②SOURCES 事件携带片段正文（RetrievalSource 增加 content 字段截断 800 字符，SSE 事件/sources_json 落库/历史接口三通道自动带出）；③前端推理卡 ReasoningCard（默认收起：阶段清单打勾+进行中 spinner、思考逐行 line-reveal、收起态末行预览、「N 个知识片段」pill）+ 知识库召回抽屉 RetrievalDrawer（右侧滑入，仅展示系统知识库召回片段）替代内联 SourcesList（组件删除）；use-chat-stream 升 11 事件状态机（stage 按序追加/同键去重）；④输入区——图片/文档合并单按钮单接口（accept 白名单合并）+ Ctrl+V 粘贴文件直接上传 + 上传按钮内嵌输入容器（图2 胶囊形态）；⑤侧栏会话行生成中脉冲动画（流式 Context 广播 streamingSessionId）；⑥导航单层化——移除深墨顶条与汉堡抽屉，首页/课程助手/课程中心三项直出顶栏并关联路由（课堂→课程中心更名）；⑦登录门控收紧——middleware PROTECTED_PREFIXES 增 /courses（仅首页公开；详情页 LoginGate 保留为过期 cookie 二层防线）；⑧首页空白块根修——Reveal 组件自带 reveal 类（此前调用方裸写类无观察器致 EntryTiles 五联画永久 opacity:0，桌面最高 920px 空白带），全站 reveal 可重复触发+方向感知（反向滚动从上沉入）；⑨课程中心学科筛选改下划线式 tab 栏、排序 select 改胶囊分段控件。测试：后端 134 相关单测绿 + 前端 384 单测绿 + E2E 同步（stage 用例/抽屉断言/门控重定向断言）。原因：用户任务指令——修复流式首字节静默与检索链路可感知性、思考/检索阶段卡片化、召回片段抽屉、统一上传、单层导航、仅首页公开、首页动效反向滚动、课程中心 tab 化筛选。
- 2026-08-27 · C 端问渠学堂全站 UI 重构 + 学员邮箱注册功能（feature/2026-08-27-c-ui-overhaul-email-register，含 PR#16 遗留提交 e128724）：①品牌定名「问渠学堂 Wenqu Academy」，全站 @theme 换肤（暖奶油+墨色+赭棕学院风）与衬线显示字体本地化；②首页按设计稿一完整还原（阅读进度条/深墨顶条+隐藏式玻璃导航/Ken Burns 巨字差速视差 Hero/能力手风琴四项/公开课程大卡保留四态与方法拼贴视差/学员声音轮播/暗景语录/指引横滑/五联宫格/AI 助教 FAB 直达真实对话页）；③独立 /login 路由组恢复（middleware 兜底重定向移除），设计稿二左右分栏还原并移除第三方登录，新增注册面板接入两段式邮箱验证码流程；④后端学员邮箱注册链路——V15 迁移 sys_user.email 列与部分唯一索引、POST /auth/register/code 与 /auth/register（验证码 6 位 15 分钟有效/60s 重发频控 SET NX EX/错误 5 次经原子 Lua 作废防爆破）、HTML 品牌邮件 RegisterMailSender（纯文本回退明文携码）、登录支持用户名或邮箱双轨识别、注册即自动登录复用 issueSession 收口；⑤SMTP 凭证 MAIL_USERNAME/MAIL_PASSWORD 环境变量注入（backend/.env 已 gitignore，零明文入库）。测试：后端单测 32 例 + 集成 5 例全绿（Testcontainers 真实 PG/Redis/Lua 与唯一索引竞态语义），前端 378 单测全绿（api.ts 维持 100% 行覆盖）+ E2E 37 例全绿。原因：用户任务指令——基于两份设计稿完成全站 UI 重构并以 TDD 上线邮箱注册，PR 含此前遗留未合并更改。
- 2026-08-26 · C 端前端改造：登录弹窗化 + 公开浏览 + 会话侧边栏管理 + 卡顿治理（feature/2026-08-26-c-frontend-ui-polish → PR 合 dev，细节见 `docs/progress/2026-08-26-c端ui改造.md`）：①登录弹窗化——独立 /login 页下线（(auth) 路由组删除，middleware 老链接兜底），全局 LoginDialog（aria-modal/焦点/Esc/遮罩）+ AuthProvider 弹窗状态机（openLoginDialog/{afterLogin}/submitLogin），401 全局回调改打开弹窗；②公开化——后端新增公开课程接口 GET /api/v1/public/courses（PublicCourseVO/PublicController/AuthConfig 排空 /api/v1/public/**/service 内置链式查询，未加缓存——量小低频避免写失效链路），首页/课堂页/课程详情页未登录可浏览（数据源换 public-courses + 登录用户「已加入」徽章交叉），课程详情页为登录门槛（自动弹窗 + 资料区登录墙 + 问 AI 助教先登录）；③会话侧边栏单一管理——增（新建无确认、流式守卫）删（ConfirmDialog 二次确认、409 分级 toast、删激活会话回 /chat）改（行内编辑标题）查（标题模糊搜索 300ms 防抖 + 清除）；删除 /sessions 页、首页「最近会话」区块、顶导「会话」导航项（会话唯一入口=课程助手侧边栏）；④后端补口——PATCH /api/v1/student/sessions/{id} 重命名（SessionRenameRequest DTO + service renameSession 复用 updateTitle 收编死代码）、GET /student/sessions 加 keyword 模糊搜索；⑤卡顿治理——(main) 路由组 template.tsx 页面入场动画（motion-safe 降级）、courses 搜索写 URL 300ms 防抖（消除每键 router.replace）、中文字体减 500 字重（400+700 保留，首屏省约 1.2MB）、首屏封面图启用 priority（首页首卡/详情 Hero）、聊天消息头像静态化（流式期间减少无限动画实例）、吸底滚动 smooth→auto。测试同步：后端 1193 测试 verify 全绿（JaCoCo 达标）、前端 349 单测全绿（覆盖率阈值达标）、E2E 重写（auth 弹窗流程/home-courses 公开断言/chat-sidebar 增删改查+sse-lifecycle 清理）。原因：用户拍板——登录页与首页简陋、会话列表应归课程助手侧边栏（增删改查+二次确认+登出确认）、首页公开可浏览而详情页为登录门槛、页面过渡与卡顿需优化。
- 2026-08-26 · Milvus sparse/BM25 检索恢复整改（TASK.md §4 实施）：content 字段加 jieba 中文分词器（`analyzerParams(type=chinese)`，Milvus 2.6 内置 jieba）解决 issue #1402 中文崩溃根因；新增 schema 版本标记机制（knowledge_chunks 创建时 collection description 写 `schema-version:2`，启动比对额外校验版本——字段名比对感知不到 analyzer 等字段属性变化，旧 collection 无标记自动 drop 重建；memory_chunks 不参与版本比对）；`retrieval.sparse-enabled=true` 还原。原因：用户 2026-08-26 改判按维护者官方用法整改（替代 2026-08-25「维持降级+季度复查」裁决，见 TASK.md §4），dev 实证通过（runAnalyzer 中文分词与官方示例一致 + 中文混合检索命中且服务端无崩溃 + 全量 verify 绿）。
- 2026-08-25 · 多实例缓存方案按用户架构指示修正（§1 实施前定稿）：弃用「Caffeine Cache 接口套 Redis 适配盒」方案（用户判定奇怪），改为缓存实现分层——简单场景（非热 key、TTL 配置化、失效可表达）走 Spring Cache 注解 + RedisCacheManager（缓存区 TTL 经 `cache.ttl.*` 配置化）；复杂场景（键前缀批量失效 / 热 key / Lua）用领域缓存类 `cache/` 目录 `{业务领域}CacheService`；Caffeine 仅限本地缓存（进程内/会话级）。落地：新建 `cache/CourseQueryCacheService`（课程查询领域缓存：单键失效 + search:/byTitle: 前缀 SCAN 批量失效 + get 一击式加载），dashboard 统计三端点改 @Cacheable 注解 + RedisCacheManager（TTL 配置化），AttachmentCacheService/PreferenceCacheService 保持 Caffeine 本地（TTL 上界接受，2026-08-25 §1 拍板）。宪法新增 A.5.4 缓存实现分层条文 + B.1 目录表 `cache/` 行（先记变更再改正文）。原因：用户 2026-08-25 架构指示——简单缓存用 Spring Cache 直接管理、复杂缓存才建领域缓存类，禁止本地缓存套 Redis 适配盒的混形。
- 2026-08-25 · TASK.md 遗留项清理（§5 调研回填 36 条 / §1/§4/§6 拍板落地）：①§5 三组（A+E/B/C+D）双源取证全量回填——29 条删行定论（含三处修正：Milvus v2.6.x 版本文档实存于 v2.6.x 分支（原 404 系查 v2.3 下划线命名）、MP 官方存在 @MybatisPlusTest 切片与 JSONB TypeHandler 专页、SAA 1.1.2 Java 侧 rerank 官方集成存在（DashScopeRerankModel + spring.ai.dashscope.rerank.*，默认 gte-rerank 随百炼 2026-05-30 下线、项目已显式配 qwen3-rerank））；7 条确认官方不可得保留注明（A-3/B-6/B-7/B-16/E-1/E-2/E-5）；宪法按实锤修订 5 处（A.3.1 补 FW7 API Versioning 注记、A.6 补 @MybatisPlusTest 切片、A.4.10 补官方 JSONB TypeHandler 路径、B.4 补 DashScope 超时实证与 rerank 集成、D.5.9 补 v2.6.2+ partial_update merge）。②§1 多实例部署用户拍板（2026-08-25）：worker-id/datacenter-id 改 env 配置化静态指定 + 课程查询缓存 Caffeine→Redis 分布式（其余缓存接受 TTL 上界），实施见后续提交。③§4 sparse 检索裁决：维持降级 + 每季度复查 milvus-sdk-java#1402（仍 Open）。④§6 拍板：B 端远程状态 vue-query 化分两批迁移（批 1 十七个手动视图 / 批 2 两混合视图补强，另立任务）、C 端学科兜底渐变已令牌化（globals.css @theme 六组 --color-subject-* 起止色对）。原因：用户指示新会话解决全部 TASK.md 遗留项。
- 2026-08-25 · B 端三处打磨（2.1-2.3）：①DocumentsView 行 ⋮ 菜单移出裁切上下文（Teleport 到 body + 按钮坐标 fixed 定位 + 滚动即收起），表格容器 overflow-hidden 行为零变化；②Feedback/Sessions 两视图内联回放 Drawer 抽公共组件 ConversationReplayDrawer（props: open/sessionId/title/initialStatus，detail 拉取与加载中关闭拦截内聚，权限差异留调用方），新增组件 5 单测；③Dashboard/Feedback ECharts 硬编码轴色/分隔线/标签/点描边十六进制统一改 @theme --color-chart-* 语义令牌（tokenColor 读取）。原因：交接清单三项打磨，行为不变零回归（B 端全量 287 单测 + 24 E2E 绿）。
- 2026-08-25 · 双前端全量 UI 重构（PR#7 C 端 / PR#8 B 端）：C 端 kimi 蓝系设计体系（暖米白底 #FBFAF9 + 品牌蓝 #2F8BF5 族）、首页电商风（分类筛选 + 滚动动效）、课程助手 kimi 式全局左栏（独立 (chat) 路由组 + 会话历史侧栏 + 大圆角输入区）；B 端深色侧栏现代管理风格（ink 石墨蓝 + 图标分组导航 + 面包屑 + 路由过渡）。原因：用户判定原界面「奇丑无比」且 C 端几乎不可用，责令全面重构；课程助手以 kimi 设计稿为参照，B 端侧栏层级与职责（学生/教师拆分、课程五子页、404、知识库入栏）按用户拍板落地；修复 RouterView+Transition 缺 :key 内容区空白、双重容器、分组展开模型反转等实证缺陷；测试与 E2E 全量同步（C 端 358 单测+29 E2E / B 端 278 单测+23 E2E，核心文件 100% 覆盖铁律保持）。
- 2026-08-25 · 宪法重构：基于 7 主题技术栈调研（`docs/agmds-research/2026-08-25-*.md`，Java+Spring Boot / MyBatis-Plus+PG / Redis+MinIO / Milvus / Spring AI Alibaba Agent / 双前端 / 构建测试 CI）按四段结构重写项目宪法，发布为 `AGENTS.md.draft` 待用户确认后替换；配套新增 CHANGELOG.md（本文件首建）；TASK.md 追加 §5 调研不可得项登记表。原因：constitution-generator 流程生成更完整、可溯源（官方文档依据）的工程规范。
- 2026-08-25 · 宪法草案审核修复（审核专员 2 轮）：Maven 本地仓库路径实测修正为 `D:/code/java/maven/apache-maven-3.9.16/repository`（旧记忆路径作废，见记忆索引）；Part C 补用户拍板前置条款（C 端界面落地前必须与用户沟通）；D.3 启动类名更正为 CommerceRagApplication；TASK.md §5 补 E-6 登记（36 项）。原因：constitution-generator §四.5 审核轮发现并修复。
- 2026-08-25 · 用户批准定稿：`AGENTS.md.draft` 替换为 `AGENTS.md` 生效（旧版备份 `AGENTS.md.bak.2026-08-25`，git 历史亦保有旧版）。
- 2026-08-25 · 默认管理员账户改配置驱动：新增 `properties/AdminSeedProperties`（`auth.admin-seed`，默认 admin/admin123，env `AUTH_ADMIN_SEED_*` 可覆盖）+ `config/AdminSeedInitializer`（ApplicationRunner，非 @PostConstruct——顺时序无竞态）+ `ISysUserService#ensureSeedSuperAdmin` 幂等写入（无超管则创建；密码仍为出厂默认则刷新为配置值，env 覆盖生效；已改密绝不覆盖）。原因：V6 迁移凭证硬编码且迁移冻结不可改，凭证改由配置挂载、启动动态注入；配 9 个新单测（seed 五分支 + 属性绑定 + 初始化器委托）。
