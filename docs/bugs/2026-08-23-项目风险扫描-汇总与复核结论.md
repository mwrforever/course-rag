# 2026-08-23 项目风险扫描——汇总与复核结论

> 主理人：ZCode 主 agent；扫描：5 个并行 subagent（B1/B2/B3 风险域 + P1/P2 性能域）；复核：2 个独立 review agent（R1 核 bug、R2 核 perf）。
> 基准：dev@afc85b2（2026-08-23）。详细证据见同日 5 份分域清单。

## 一、复核结论总览

### 风险清单（19 项，docs/bugs/2026-08-23-项目风险扫描-*.md）

| 编号 | 严重度 | R1 判定 | 一句话结论 |
|---|---|---|---|
| B1-1 | 高 | 实锤 | PG 降级互踢踢中刚创建的 ACTIVE 记录，newLoginId 形参未使用，Redis 故障期间登录即自吊销 |
| B1-2 | 中 | 实锤 | RT 旋转后 login_record 更新 0 行静默脱节，登出/禁用吊销落空；refresh 不校验用户禁用状态 |
| B1-3 | 低 | 实锤 | SearchKnowledgeTool.searchExecutor 全仓唯一无 @PreDestroy 关闭的业务线程池 |
| B1-4 | 低 | 部分属实（修正） | 机制属实但**手动清理接口已存在**（AdminLoginRecordController cleanupExpiredBlacklist）；真实缺口=无自动定时清理 + countByJti 不过滤 expires_at |
| B1-5 | 低 | 实锤 | RerankService 调用 @Deprecated record 组件 source()，违宪（deprecation 清零） |
| B2-1 | 高 | 实锤 | runPool 队列满拒绝分支不推终态事件不清 ring，客户端永久"生成中"，重连不可恢复 |
| B2-2 | 高 | 实锤 | reparse 无状态守卫重置 PENDING 绕过 ETL CAS 抢占，同文档双管道并发互踩 |
| B2-3 | 高 | 实锤 | run 滞留 QUEUED 零回收（巡检只扫 ACTIVE），常规滚动重启即会话永久 409 锁死 |
| B2-4 | 中 | 实锤 | 完成时刻 DB 故障残余路径：双终态事件 + 二次 persistMessages 重复落库（无 (run_id,seq) 唯一约束兜底） |
| B2-5 | 中 | 实锤 | 四处多表级联软删无 @Transactional，中途失败留部分级联 |
| B2-6 | 中 | 实锤 | evictCourse 漏失效 byTitle:* 键族（与 P1-2 同一问题，修复合并） |
| B2-7 | 低 | 实锤 | cancel 无终态校验，对已终态 run 取消时 cancelFlags 条目永久泄漏 |
| B2-8 | 低 | 实锤 | 五处 check-then-insert 撞唯一索引竞态落 503 而非 409（feedback 侧由 P1-5 upsert 一并解决） |
| B3-1 | 高 | 实锤 | ETL Milvus 插入失败吞异常，failedCount 不增照常标 INDEXED，向量永久缺失漏召回 |
| B3-2 | 中 | 实锤 | 附件 objectKey 客户端可控且下载无归属/前缀校验，可直读同 bucket 任意对象 |
| B3-3 | 中 | 实锤 | 检索 document 块"来源文档"恒"未知"，docTitle 全链路从未填充，偏离 spec §3.2 |
| B3-4 | 中 | 实锤 | 附件局部检索 embed 无降级捕获，embedding 故障 + 文档附件时整个 run ERROR |
| B3-5 | 中 | 实锤 | SOURCES 事件全链路零发送 + sourcesJson 恒 "[]"，SSE 契约缺口（f59d322 删产生分支，无移除裁决） |
| B3-6 | 低 | 实锤 | WarningHook fallbackWarnings 组件级全局缓冲，降级告警可跨用户串扰注入他人上下文 |

### 性能清单（17 项，docs/pref/2026-08-23-性能优化-*.md）

R2 判定：**可落地 11 项**（P1-4 与 P2-4 为同一问题合并；B2-6=P1-2 与 bug 合并）、**属实但留人工拍板 5 项**、误报 0。方案修正两处：

- P1-3：dense_vector 投影裁剪**必须保留 metadata_json 列**（DocumentChunkVO 有该字段，清单误称未消费）；向量大小实为 1024 维约 4KB/行（清单写 1536 维偏大，方向正确）。
- P1-4/P2-4：批插**必须走 mapper XML batchInsert 路线**（P1-4 原方案注入 IDocumentChunkService 与 DocumentChunkServiceImpl 已注入 EtlPipeline 构成循环依赖，违宪）。

| 编号 | R2 判定 | 一句话结论 |
|---|---|---|
| P1-1 | 可落地 | partial 索引把 course_id='DEFAULT' 行排除在索引外，J3/J2 分页全表扫；新增 partial 索引 DDL |
| P1-2 | 可落地（=B2-6） | evictCourse 补 byTitle:* 失效 |
| P1-3 | 可落地 | B 端分片分页去掉 dense_vector 全列取回（保留 metadata_json） |
| P1-4/P2-4 | 可落地 | ETL 分片落库逐条 insert 改 XML batchInsert；顺带 process 链 4 次 selectById 收敛 |
| P1-5 | 可落地 | 反馈 selectOne+insert/update 两往返改 ON CONFLICT 单条 upsert（顺带消并发 DuplicateKey） |
| P1-6 | 可落地 | 单分片四端点同行双查消除 + selectBatchIds 投影 |
| P1-9 | 可落地（低收益） | addTeachers 逐条 insert 改批插 |
| P1-10 | 可落地（低收益） | B 端 findContext 相邻分片 3 次全列 selectById 改批量+投影 |
| P2-1 | 可落地 | 附件文档逐块串行 embed 改批量接口（对话关键路径收益最大） |
| P2-2 | 可落地 | 附件下载/图片 caption 串行改独立附件池并行（宪法明文要求） |
| P2-3 | 可落地 | RetrieveNode 并行度 2→3（最重的知识检索不再排队） |
| P1-7 | 留拍板 | J2 课程资料列表无分页——List→分页是 API 契约变化，需前端/产品确认交互形态 |
| P1-8 | 留拍板 | batchUpdateContents 收敛校验/失效——低频 + 收敛后循环中途异常会漏失效，需 finally 兜底设计，性价比低 |
| P2-5 | 留拍板 | B 端标注同步 embed/Milvus 异步化——引入检索一致性窗口与失败反馈通道，属产品语义决策（08-16 用户只采纳批量化先例） |
| P2-6 | 留拍板 | ETL 链路显式超时体系——SDK 默认超时行为需挂起模拟实测后定值；document 中间态巡检可另行立项 |
| P2-7 | 留拍板 | Tika 图片字节堆驻留——无实测内存数据前无法定级；过滤前置需确认 parser 容忍缺失图片 |

## 二、本次修复范围（fix/2026-08-23-risk-scan，每问题一提交）

- 风险 19 项全部修复：18 项首轮修复 + B2-2 在 code-review 环节发现首轮分组遗漏后补修（10a9602）。
- 性能可落地项全部修复（B2-6 与 P1-2 合并一提交；P1-4 与 P2-4 合并一提交）。
- code-review 追加修复：V13 重复数据兼容（a7c9eca）、B2-1 Ring 优雅关闭补遗（8ca1f34）、SOURCES payload 契约补记（461cd03）。
- 留拍板 5 项（P1-7/P1-8/P2-5/P2-6/P2-7）不在本次范围，理由见上表；另有历史已知待拍板项 3-2（记忆提取多实例防抖）维持不动。
- 流程：superpowers TDD（每项先红测后实现），修复 subagent 按文件域分组在独立 worktree 并行作业，主 agent 审查 diff 后逐项提交；全量门禁（spotless/checkstyle/spotbugs/jacoco）+ 1125 测试全绿 + code-review 复核通过后合并。

## 三、修复记录（fix/2026-08-23-risk-scan，dev 基线 afc85b2，共 36 提交：29 首轮 + 5 code-review 补修 + 2 覆盖率/文档校准）

| 编号 | 提交 | 说明 |
|---|---|---|
| docs | 716c4f2 | 扫描清单 + R1/R2 复核结论 + 修复范围裁决 |
| B1-2 | b528076 | refresh 0 行脱节抛 401 + 用户 ACTIVE 校验 |
| B1-4 | 4cd8399 | countByJti 过期过滤 + 定时清理调度（R1 修正口径） |
| B1-1 | 4f63aa7 | selectActiveForUpdate 排除当前登录 |
| B2-7 | ed0843a | cancel 终态校验 409 |
| B2-1 | f0783f0 | 拒绝分支终态三件套 |
| B2-3 | e337aff | QUEUED 滞留回收 + 启动首扫 |
| B2-4 | cff9249 | 双终态防护 + V13 唯一索引 + persistMessages 幂等 |
| B3-1 | 1cf2583 | Milvus insert 上抛消误标 INDEXED |
| P1-4/P2-4 | 43a0999 | ETL 批插 + 链双向回填 + TransactionTemplate + selectById 收敛 |
| B2-5 | a11cd64 | 四处级联软删 @Transactional |
| P1-9 | 7980c73 | addTeachers 批插（含 B2-8 course 侧兜底与用例） |
| B2-8 | 7273ccc | 用户名/KB.create 竞态兜底 409 |
| P1-1 | 7b5da2b | V14 partial 索引 |
| B2-6/P1-2 | 8e19153 | evictCourse 补 byTitle:* |
| P1-5 | 32c933e | 反馈 ON CONFLICT upsert |
| P1-3+P1-6+P1-10 | 695ace4 + bfb38a4 | B 端分片查询收口（同文件交织合并 + XML 补遗） |
| B3-2 | 04abe62 | 附件下载前缀校验 403 |
| P2-1 | aa3ed4c | 附件文档批量向量化 |
| P2-2 | 56b5237 | 附件下载/caption 并行化（附件侧） |
| B1-5 | b5beea7 | 消除 @Deprecated source() |
| B3-6 | ff60eea | WarningHook 串扰修复 |
| P2-2b | 70ab6cc | ETL 图片并行化（P2-2 的 ETL 部分） |
| B1-3 | 700fedc | 检索池 @PreDestroy + 线程序号 |
| B3-3 | c1e7b5f | docTitle 批量回查填充 |
| B3-4+B3-5+P2-3 | d5cf100 + 2f787da | 检索链路三项（同文件交织合并 + yml 补遗） |
| P1-5 补遗 | aef26dc | IdentifierGenerator Bean 注册（修复 70 集成测试上下文失败） |
| B2-2 | 10a9602 | reparse CAS 状态守卫（code-review Critical 发现首轮遗漏，补修） |
| V13 补遗 | a7c9eca | 迁移内先软删历史重复行再建唯一索引（code-review Important） |
| B2-1 补遗 | 8ca1f34 | Ring.close 优雅 drain，ERROR 终态事件真实送达（code-review Important） |
| 契约补记 | 461cd03 | SOURCES payload 定义 + orTimeout 语义注释（code-review Minor） |
| 覆盖率补齐 | 165564b | 两类补 10 用例至 100%（jacoco 单类门禁） |

**合并提交说明**：P1-3/6/10 与 B3-4/5/P2-3 两组因同文件方法簇深度交织（拆分无法构造可编译中间态）合并提交，message 已列明全部编号；其余每问题一个提交。
**过程中修复**：P1-5 的 IdentifierGenerator 注入在第一波 verify 中暴露 Spring 上下文启动失败（MP 3.5.12 自动配置不注册该 Bean），aef26dc 补注册。
**留拍板项（本轮不修）**：P1-7（J2 分页 API 契约变化）、P1-8（batchUpdateContents 收敛）、P2-5（B 端标注异步化语义）、P2-6（超时体系需实测）、P2-7（图片字节驻留需实测）、3-2（多实例防抖，历史项）。
**范围外发现（未修，供下轮）**：DocumentChunkServiceImpl:542/552 调用 MP 已弃用 selectBatchIds（历史遗留非本次引入）；RetrieveNodeTest:74 声明未用字段（历史死代码）。
