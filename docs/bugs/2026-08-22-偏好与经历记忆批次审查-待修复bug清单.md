# 2026-08-22 偏好与经历记忆批次审查 — 待修复 bug 清单

## 审查范围与口径说明

- **时间口径**：最近 24 小时（2026-08-21 至今）仓库无新提交（工作区干净，HEAD=`da478f1`）。
  本次审查对象为**最近一批代码变更**：2026-08-20 09:12–19:15 的 S1 计划 4（用户偏好记忆）+ 计划 5（经历记忆）系列提交，
  commit 范围 `e94f626^..1f080fc`（28 个提交，主代码 44 个文件，+3570 行）。
- **审查方式**：只读分析。双子 agent 分域（经历记忆域 / 偏好记忆域）识别候选问题，主 agent 逐条对照当前 HEAD 代码与 git diff 核实后收录。所有问题均附仓库可验证证据；证据不足的疑点单独列附录，不计入正式清单。
- **结论概览**：未发现"高"严重度问题（无运行即崩错误、无跨用户数据泄漏、无资金/权限类缺陷）；确认 **5 中 + 7 低** 共 12 项待修复问题，集中于数据质量退化、批内竞态与宪法规范违反。

---

## 一、中严重度（5 项）

### BUG-01｜偏好/经历双通道共用 2 线程调度器，「互不阻塞」承诺不成立

- **类别**：A 运行风险 + F 宪法违反（「各业务独立线程池……禁止共用一个池导致互相阻塞」）
- **位置**：`backend/src/main/java/com/commerce/rag/service/MemoryExtractionPipeline.java:102-118`（线程池创建）、`:146`（偏好调度）、`:245-246`（经历调度）、`:173/179/193`（偏好 DB 读 + `future.get` + 事务写）、`:275/281/292`（经历同款）；配置 `backend/src/main/resources/application.yml:317`（`threads: 2`）
- **代码证据**：

  ```java
  // L102-106：偏好/经历两通道共用同一 scheduler（默认 2 线程）
  this.scheduler = Executors.newScheduledThreadPool(threads, r -> {
      Thread t = new Thread(r, "memory-extract-");
      ...
  // L179：executeInternal 在 scheduler 线程上阻塞等待 LLM 结果（至多 10s）
  result = future.get(timeoutMs, TimeUnit.MILLISECONDS);
  // L193：事务写也在 scheduler 线程上执行
  int written = preferenceService.applyExtraction(...);
  ```

  类注释（L30-31、L44-45）自述「偏好/经历各独立执行器……互不阻塞」，但**独立执行器只隔离了 LLM 调用本身**；`future.get(10s)` 等待、DB 读、`@Transactional` 事务写全部占用共享调度器线程。
- **Commit 证据**：`ac4f390` 引入单通道（DB/阻塞已在调度线程）；`87792f8` 扩展双通道并作出「互不阻塞」声明。
- **影响**：LLM 故障/超时（单次占用调度线程至多 10s）或 DB 慢查时，2 个调度线程被占满，另一通道及其它用户的防抖到期任务全部延迟触发（pending 保留不丢失，但批次时效退化）。违反 spec §8.4「两流水线互不阻塞」自述目标。
- **核实状态**：✅ 已对照 HEAD 代码逐行确认。

### BUG-02｜同批 LLM 候选「先全部决策、后统一执行」：episodic 批内去重与 merge_target 定位全部失效

- **类别**：B 数据质量
- **位置**：`backend/src/main/java/com/commerce/rag/service/impl/EpisodicMemoryServiceImpl.java:85-105`（决策/执行两段循环）；决策规则在 `service/EpisodicDecisionEngine.java:69`（CREATE 去重）、`:113-117`（INVALIDATE 未命中 → IGNORE）
- **代码证据**：

  ```java
  List<EpisodicAction> actions = new ArrayList<>();
  for (EpisodicMemoryExtraction memory : result.memories()) {
      List<UserEpisodicMemory> rows = this.lambdaQuery()...list(); // 全部决策先读 DB（批前快照）
      actions.add(decisionEngine.decide(memory, rows));
  }
  for (EpisodicAction action : actions) {
      written += execute(userId, sourceSessionId, action);          // 写入发生在全部决策之后
  }
  ```

  决策引擎只看 DB 既有行，看不到本批已决策未执行的写入。
- **Commit 证据**：`c6d7438` 引入该结构。
- **影响**：LLM 单批返回多条关联条目时（`episodic_memories` 允许多条）：
  1. 同 type 同 content 两条 CREATE → 去重规则被绕过，落两条完全相同的 active 行，永不再被清理；
  2. 批内 CREATE「X」+ INVALIDATE merge_target=「X」（用户同轮先陈述后否认）→ 目标行尚不存在 → IGNORE，被用户明确否定的事实以 active 永久留存；
  3. 两条 UPDATE 指向同一旧行 → 各建一条 active 新行且 version 相同。
  后果为记忆表数据质量退化并直接进入召回注入。
- **核实状态**：✅ 已确认两段循环结构与决策引擎规则匹配。

### BUG-03｜偏好同批候选脏读：单值 key 批内冲突触发唯一索引违例，整批（含删除与其它 key 有效变更）全部回滚

- **类别**：B 数据丢失
- **位置**：`backend/src/main/java/com/commerce/rag/service/impl/PreferenceServiceImpl.java:52-88`（deletions 先执行 → 全候选决策 → 统一执行）；`backend/src/main/resources/db/migration/V11__user_preference.sql:30-38`（两个部分唯一索引）
- **代码证据**：

  ```java
  // 决策阶段：两个同 key 候选看到同一批前快照（均无 active 行）→ 均产出 CREATE_ACTIVE
  for (PreferenceCandidate candidate : result.candidates()) {
      List<UserPreference> rows = this.lambdaQuery()...list();
      actions.add(decisionEngine.decide(candidate, rows));
  }
  // 执行阶段：两条 CREATE_ACTIVE 落库 → 第二条撞 uk_user_pref_single_active → 整个事务回滚
  for (PreferenceAction action : actions) {
      written += execute(userId, action);
  }
  ```

  ```sql
  -- V11 L30-33：单值 key 同 (user_id, key) 仅一行 active
  CREATE UNIQUE INDEX uk_user_pref_single_active ON user_preference(user_id, key)
      WHERE deleted = 0 AND status = 'active'
        AND key IN ('response_language', 'response_verbosity', 'explain_depth');
  ```

  异常被 `MemoryExtractionPipeline.executeInternal` 的 catch（L196-199）捕获后「丢弃本批」。
- **Commit 证据**：`aa5c040` 引入 decide-all/execute-all 结构；流水线 `MemoryExtractionPipeline.java:190-191` 注释自认「单值唯一索引冲突由 applyExtraction 事务原子回滚」。
- **影响**：LLM 一批输出同 key 矛盾值（如 response_language 同时给「英文」+「中文」）时，`@Transactional` 整体回滚——本批内**其它 key 的有效偏好、REINFORCE、以及第 1 步已执行的 DELETE 意图全部丢失**，无重试。爆炸半径大于必要（理想应只牺牲冲突候选）。
- **核实状态**：✅ 已确认决策/执行顺序、deletions 前置执行、V11 唯一索引覆盖范围。

### BUG-04｜deletions 候选不做 value 归一化，与 candidates 处理不对称：用户撤回偏好的软删可能永不命中

- **类别**：B 数据质量
- **位置**：`backend/src/main/java/com/commerce/rag/service/PreferenceExtractionService.java:126-130`（候选归一化）vs `:134-144`（删除原样透传）；`service/impl/PreferenceServiceImpl.java:155-161`（softDelete 精确匹配）
- **代码证据**：

  ```java
  // 候选：value 经 normalizeValue 归一化落库（L128）
  candidates.add(new PreferenceCandidate(key, normalizeValue(key, value), ...));
  // 删除：无归一化，原样透传（L141）
  deletions.add(new PreferenceDeletion(key, value));
  ```

  ```java
  // softDelete 按 value 精确匹配（库里存的是归一化规范值）
  .eq(UserPreference::getUserId, userId)
  .eq(UserPreference::getKey, key)
  .eq(UserPreference::getValue, value)
  ```

- **Commit 证据**：`dc9340e` 引入（原始 diff 中 deletions 分支即无 normalizeValue）；后续 `100abdb` 仅收窄 catch 未修。
- **影响**：落库值均为归一化规范值（如 response_language 存「中文」），而删除意图若 LLM 输出同义变体（"chinese"/"en"/"简短"），精确匹配未命中 → 用户明确撤回的偏好被静默保留（仅 info 日志「删除未命中」），继续注入 prompt。归一化兜底对候选生效、对删除失效，属实现不一致。
- **核实状态**：✅ 已确认两分支不对称 + softDelete 等值匹配。

### BUG-05｜findActiveMemoriesText 无界查询 + {existing} 全量拼接注入提取 prompt，记忆量增长后提取链路静默停摆

- **类别**：A 运行风险
- **位置**：`backend/src/main/java/com/commerce/rag/service/impl/EpisodicMemoryServiceImpl.java:250-257`（无界查询）+ `:264-274`（全量拼接）；消费点 `service/MemoryExtractionPipeline.java:275`（每个防抖窗口调用一次）
- **代码证据**：

  ```java
  public String findActiveMemoriesText(Long userId) {
      List<UserEpisodicMemory> active = this.lambdaQuery()
              .select(UserEpisodicMemory::getType, UserEpisodicMemory::getContent)
              .eq(UserEpisodicMemory::getUserId, userId)
              .eq(UserEpisodicMemory::getValidity, "active")
              .list();                       // 无 limit/分页，完全无上限
  }
  // toExistingMemoriesText：每行「标签: 内容」全量拼接（content VARCHAR(2000)）
  ```

  注：`MyBatisPlusConfig` 的 `PaginationInnerInterceptor maxLimit=2000` **只对分页查询生效**，本处非分页 `.list()` 不受任何截断保护。
- **Commit 证据**：`c6d7438` 引入 findActiveMemoriesText；`87792f8` 将其接入每个提取批次。
- **影响**：user_episodic_memory 同 type 可多行（V12 无单值约束），active 行随时间单调增长。累积至数百条后 `{existing}` 注入文本远超提取模型合理上下文 → `extract` 超时/解析失败 → 静默降级返回空（`EpisodicExtractionService` 仅 warn 日志），**该用户经历记忆提取永久停摆且无告警**，且每个防抖窗口重复支付巨型 prompt 成本。
- **核实状态**：✅ 已确认无分页无上限；已修正子 agent 关于「maxLimit=2000 兜底」的错误表述（该拦截器不适用于非分页查询）。

---

## 二、低严重度（7 项）

### BUG-06｜@Transactional 事务内执行 embedding 与 Milvus 远程调用，DB 连接被外部 RTT 持有

- **类别**：D 资源持有
- **位置**：`backend/src/main/java/com/commerce/rag/service/impl/EpisodicMemoryServiceImpl.java:77-78`（@Transactional）→ `:123/138-139/148`（事务内 syncIndexBestEffort）→ `:336`（`embeddingModel.embed` 远程 HTTP）+ `:410`（`milvusClientV2.upsert`）
- **代码证据**：

  ```java
  @Override
  @Transactional
  public int applyExtraction(...) {
      ...
      case CREATE -> {
          save(row);
          syncIndexBestEffort(() -> buildUpsert(row, "active")); // buildUpsert → embed（远程）→ upsert（远程）
      }
  ```

- **Commit 证据**：`c6d7438` 引入。
- **影响**：每个动作在未提交事务内做 1~2 次 embedding HTTP 调用 + Milvus upsert（UPDATE/MERGE 还有 buildUpsertById 反查+embedding），事务及已持有 PG 连接的时长 = Σ(外部 RTT)，且不受流水线 `timeoutMs=10s` 约束（超时只管 LLM 提取调用）。episodicExecutor 仅 2 线程限制了并发上限，常态影响小；embedding 服务劣化时连接被长时间占用，与主链路共用 Hikari 池，存在拖慢主链路的风险。索引先于事务提交写入的幻影行会被 recall 的 PG 回查过滤，无正确性问题，仅资源持有问题。
- **核实状态**：✅ 调用链逐行确认。
- **修复方向**：事务内只做 DB 写并收集待同步行，事务提交后（TransactionSynchronization afterCommit 或拆方法）再 best-effort 同步索引。

### BUG-07｜偏好与经历 applyExtraction 循环内逐条单查既有行（N+1 模式），违反宪法

- **类别**：F 宪法违反（「拒绝 N+1 查询：循环内单查改批量查询」）
- **位置**：`backend/src/main/java/com/commerce/rag/service/impl/PreferenceServiceImpl.java:62-77`；`service/impl/EpisodicMemoryServiceImpl.java:87-99`
- **代码证据**：

  ```java
  for (PreferenceCandidate candidate : result.candidates()) {
      List<UserPreference> rows = this.lambdaQuery()   // 每候选一条 SELECT
              .select(...).eq(UserPreference::getUserId, userId)
              .eq(UserPreference::getKey, candidate.key()).list();
      actions.add(decisionEngine.decide(candidate, rows));
  }
  ```

- **Commit 证据**：`aa5c040`（偏好）、`c6d7438`（经历）分别引入。
- **影响**：一批 N 条候选发 N 条 SQL，同 key/type 条目重复发相同 SQL（且查回全部历史行，决策才在内存过滤 active）。单批条数为个位数，实际影响小，属可验证的规范违反。可按 distinct key/type 一次 `in` 查询后内存分组组装。
- **核实状态**：✅ 两处循环均确认。可与 BUG-02/BUG-03 的批内快照问题一并重构（批量取行 + 内存决策 + 逐条执行可见前序写入）。

### BUG-08｜偏好提取 system 提示词携带字面 `{existing}` 占位符发给模型

- **类别**：A 提示词缺陷
- **位置**：`backend/src/main/java/com/commerce/rag/service/PreferenceExtractionService.java:66`（system 未做 replace）+ `backend/src/main/resources/prompts/memory-extraction.yml:35`
- **代码证据**：

  ```java
  String system = sections.getOrDefault("memory-extraction.system", "");   // 无任何占位符替换
  String instruction = ... .replace("{context}", ...) ... .replace("{existing}", ...); // 只替换 instruction
  ```

  ```yaml
  # memory-extraction.yml system 段 L35：
  - 若 {existing} 中已有同义 value,复用已有 value,不输出新值
  ```

- **Commit 证据**：`dc9340e` 同时引入 yml 与加载代码（对照：episodic 域同类问题已在 `34612f5` 修复，偏好域遗漏）。
- **影响**：模型在 system 中看到悬空占位符 `{existing}`，开放型 key 同义收敛指令失去指代（真实已有偏好在 instruction 的 `<existing>` 段，故仅为提示词质量降级，不影响解析）。
- **核实状态**：✅ yml 原文与加载代码均确认。

### BUG-09｜提取/调度线程池为无界队列 FixedThreadPool，且池内线程全部同名（无序号）

- **类别**：D 资源配置
- **位置**：`backend/src/main/java/com/commerce/rag/service/MemoryExtractionPipeline.java:102-118`
- **代码证据**：

  ```java
  this.extractionExecutor = Executors.newFixedThreadPool(threads, r -> {
      Thread t = new Thread(r, "memory-extract-call-");   // 所有线程同名，无 -N 序号
      t.setDaemon(true);
      return t;
  });                                                     // LinkedBlockingQueue 无界
  ```

- **Commit 证据**：`ac4f390`（scheduler + extractionExecutor）、`87792f8`（episodicExecutor 同模式）。
- **影响**：LLM 服务端故障时 `future.get` 超时后靠 `cancel(true)` 中断释放线程；中断不生效的极端场景下任务在无界队列堆积。防抖 latest-wins 将每用户每通道限制为 1 个 pending，实际上界≈活跃用户数，故降为低。线程同名导致 thread dump 无法区分池内线程，排障困难。
- **核实状态**：✅ 确认。可改用 `AtomicInteger` 编号 ThreadFactory + 有界队列（`ThreadPoolExecutor` 指定 queue 容量）。

### BUG-10｜recall 检索 `ef=64` 硬编码，`milvus.hnsw-ef` 配置键已定义却零消费

- **类别**：F 宪法违反（「阈值等配置数值归 properties/（全配置化）」）
- **位置**：`backend/src/main/java/com/commerce/rag/service/impl/EpisodicMemoryServiceImpl.java:186`；`backend/src/main/resources/application.yml:127`
- **代码证据**：

  ```java
  .searchParams(Map.of("ef", 64))     // 硬编码
  ```

  ```yaml
  milvus:
    hnsw-ef: 64        # 已定义但主代码零消费（grep 全仓仅命中 hnsw-ef-construction）
  ```

- **Commit 证据**：`c6d7438` 引入（既有 `SearchKnowledgeTool.java:68` 的 `"{\"ef\": 64}"` 为历史先例，新代码延续违例而非引用配置）。
- **影响**：调优 ef 需改代码重发；配置键形同虚设会误导运维。运行上无错误。
- **核实状态**：✅ grep 全仓确认 `milvus.hnsw-ef` 无任何消费点。

### BUG-11｜偏好写库后不失效 30min 冻结缓存——对宪法「先写 DB → 后失效缓存」一致性铁律的文档化偏离

- **类别**：F 宪法偏离（有注释说明的有意设计，提请确认产品可接受性）
- **位置**：`backend/src/main/java/com/commerce/rag/service/PreferenceCacheService.java:16-20`（设计声明）、`:49-59`（getOrBuild）
- **代码证据**：

  ```java
  * <p>一致性：写偏好后不主动失效缓存（30min 到点自然过期拉新）——设计即「冻结一段时间 +
  * 定期同步」，偏好一变 prompt 立即变反会持续破坏 prefix cache。
  public String getOrBuild(Long userId) {
      String cached = cache.getIfPresent(userId);   // 写库后 30min 内持续返回旧偏好块
  ```

- **Commit 证据**：`54e5fc2` 引入（spec §7.8 防 prefix cache 破坏）。
- **影响**：用户明确说「以后用英文回答」后，最长 30min 内注入仍是旧偏好（中文），产品感知为「改了没生效」。缓存有界（maximumSize=256）+ 有 TTL，非永久缓存。因是有文档的权衡而非疏漏，列为低；若产品侧不可接受，需在「prefix cache 收益」与「偏好生效时效」间重新裁决。
- **核实状态**：✅ 设计注释与实现一致确认。

### BUG-12｜（伴随 BUG-02 的同构问题）episodic 批内 UPDATE/MERGE 目标行 version 演算基于同一快照

- **类别**：B 数据质量（BUG-02 的具体子场景，独立列出便于修复验证）
- **位置**：`backend/src/main/java/com/commerce/rag/service/EpisodicDecisionEngine.java:101-111`（version = target.getVersion() + 1）
- **代码证据**：

  ```java
  return new EpisodicAction(
          ...,
          target.getId(),
          target.getVersion() + 1,   // 两条 UPDATE 指向同一旧行时，各自产出的新行 version 相同
          ...);
  ```

- **Commit 证据**：`5ad1b36` 引入决策引擎；`c6d7438` 接入批处理结构。
- **影响**：同批两条 UPDATE/MERGE 命中同一旧行时：旧行被两次状态流转（幂等无害），但产出两条 version 相同的 active 新行，版本链审计失真（无法从 version 判断真实演进顺序）。修复 BUG-02（执行可见前序写入或批内去重定位）后本问题随之消解。
- **核实状态**：✅ 与 BUG-02 同源确认。

---

## 三、已排查确认无问题的重点项

- **user_id 硬隔离（权限绕过类，重点核查）**：recall 的 Milvus filter 带 `user_id == "{userId}"`（Long 解析后拼接，无注入面）、PG 回查追加 `.eq(userId)`（`c0687e6` 已补）；偏好/经历全部读写路径带 userId 过滤；拦截器读取的 refs 来自 per-request context，无跨请求泄漏；worker 的 metadata["userId"] 唯一写入方为 `ChatRequestWorker`（`Long.parseLong` 成功后的数字字符串）。
- **提示词占位符主链路**：episodic-extraction.yml 的 `{context}/{current}/{existing}` 与代码三个 replace 一一对应（`34612f5` 已修复 episodic 域同款问题）；偏好域 instruction 段匹配（system 段遗留见 BUG-08）。
- **RetrieveNode 召回查询**：取首个非空重写项（`1f080fc`），前置判空完整无 NPE 路径；`c605207` catch 收窄后降级链完整。
- **MilvusCollectionInitializer 双集合**：knowledge_chunks 14 字段口径未回归；memory_chunks 6 字段 schema 与写入严格一致；异常降级不阻断启动。
- **V12 迁移/实体/@TableLogic 软删**：与 V11 先例一致；TIMESTAMPTZ 列已按需取列避开；索引覆盖两条查询路径。
- **SSE/接口契约**：本批未改任何 controller 契约、SSE 事件 schema 或前端依赖字段；episodic/preference 注入为模型侧瞬时消息，不进持久化消息。
- **防抖并发**：pending/futures 均 ConcurrentHashMap，latest-wins + take-once 语义，无消息丢失或双跑。
- **事务与软删执行顺序**：PROMOTE/UPDATE 先 `removeById` 软删旧行（出部分唯一索引）再插入，顺序正确。

## 四、低置信疑点（未列入正式清单，供后续观察）

1. **memory_chunks schema 演进风险**：schema 不匹配时 initializer 会 drop 重建集合，清空全部记忆向量；PG 为事实源但无 reindex 任务，已存记忆将漏召回。当前 schema 匹配不会触发，属未来演进风险。
2. **submitEpisodic 的 sessionId 竞态**：防抖到期瞬间的新 submit 可能使旧任务消费新批次消息并以旧 sessionId 落库（source_session_id 归属偏差）。窗口极窄、仅影响审计字段，未能证明实际可触发。
3. **MemoryExtractionInputAssembler.lastAssistantAfter** 取全列表最后一条 AssistantMessage，未限定「最后一条 UserMessage 之后」；仅 run COMPLETED 且无尾部回答的异常形态可达，正常链路不可达。
4. **降级日志不带堆栈**：两个 executeInternal 的 catch 仅 `e.getMessage()`，排障信息偏少，属有意降级设计。

---

*审查方法：双子 agent 分域识别（general-purpose，只读）→ 主 agent 对当前 HEAD 逐条核实代码与 commit 证据 → 仅收录可验证问题。生成时间：2026-08-22。*
