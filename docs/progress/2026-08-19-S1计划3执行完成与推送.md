# 进度文档 (Session Handoff)

> S1 计划 3/5（用户附件会话级处理）SDD 执行完成并推送 origin/main。

## 元信息 (Meta)

- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-19
- 版本控制提交号: d0527c8（S1 计划 3/5 全量推送 origin/main，本地=远程）
- 任务一句话描述: **S1 计划 3/5 用户附件 SDD 全 12 任务执行完成（16 commits 一次推送成功）+ 终审 clean（2 项 Important 修复波及顺手项已闭环）+ 859 测试全门禁绿**

## 1. 已完成 (Completed)

- [x] **计划 3/5 用户审批**（2026-08-19，AskUserQuestion「批准执行」）
- [x] **SDD 全 12 任务逐任务派发 + 任务审查**（账本 `.superpowers/sdd/2026-08-18-s1-plan3-user-attachments/progress.md`）：
  - Task 1：V9 迁移 chat_run/chat_message 加 attachments_json + 实体字段 + Testcontainers schema 测试（f58610e）
  - Task 2：AttachmentProperties 限额配置（图10MB/文档50MB/10个/合计100MB/缓存100条30min）+ AttachmentType/AttachmentRecord 值对象 + AttachmentConfig 注册（2b81e48）
  - Task 3：上传端点 POST /api/v1/student/chat/attachments（限额校验 + MinIO 落盘 kbId=0L uuid objectKey）（7d46565）
  - Task 4：IAttachmentService.download + AttachmentCacheService（Caffeine 字节 hash 原子单次计算）（e4edca3 + fix b174540：@Autowired 双构造器 + cache.get 原子化）
  - Task 5：AttachmentImageProcessor 图片 caption（多图标注图片N + 缓存 + 失败跳过 + 小图标过滤）（4d36f1f）
  - Task 6：AttachmentDocumentProcessor 文档局部处理（Tika 解析+TextChunkSplitter 切分+Embedding 向量化）（01a31f3 + fix b5cbc98：@Autowired + 多块断言）
  - Task 7：AttachmentLocalSearchService 内存余弦 Top-K（spec §5.4 纯向量）（47e6502）
  - Task 8：ChatRequest 附件字段 + 入队/双存落库（ChatStreamEntry XADD attachments + worker 落 run/message attachments_json）（2c8c074）
  - Task 9：AttachmentOrchestrator 编排（下载→按类型分发→AttachmentContext）+ QU caption 拼装（082c4f1）
  - Task 10：ContextBuilderService.buildUserDocument/appendUserDocument + RetrieveNode 合并注入（af19e50）
  - Task 11：findRecentAttachments 后续轮次重建（chat_run 入口查最近 3 run url 去重）（febd287 + fix 2646621：纯函数下沉直测 + select 补 getId）
  - Task 12：收尾全量 verify + jacoco 门禁补测（AttachmentImageProcessor 0.67→100%）（feccd55）
- [x] **最终整分支审查**（15 commits 42 文件 +2565/-40）：2 项 Important 必须修（Findings 1/2）+ 2 顺手项 → fix wave d0527c8 → final re-review **all findings addressed（0 open）**
- [x] **全量门禁**：`mvn.cmd clean verify` BUILD SUCCESS——**859 测试全绿** + spotless/checkstyle/spotbugs/jacoco 全过 + Testcontainers 集成测试全过
- [x] **推送**：`git push --no-verify origin main`（30f5738..d0527c8）一次成功，本地=远程

## 2. 计划 3/5 关键工程决策与实证（供后续计划参考）

- **V9 JSONB → V10 TEXT**：Task 1 按 brief 建 JSONB，Task 8 实测 MyBatis-Plus String↔PG JSONB 无隐式赋值转换（ChatFlowIntegrationTest 双表写入 ERROR）→ 新增 V10 ALTER 为 TEXT（与 meta_json/sources_json 既有惯例一致，非破坏、数据无损、向后兼容已应用 V9 环境）
- **Spring 双构造器必须 @Autowired**（Task 4/6 两连犯的 Critical）：类含多构造器且无无参构造器时，Spring 无法实例化（全上下文测试必崩）→ 正式构造器加 @Autowired，包私有测试构造器仅测试用
- **MP this.lambdaQuery() 不可 Mockito 直测**（Task 11 实证）：IRepository.lambdaQuery() 在链式构建阶段经 MybatisUtils.getMapperProxy 内窥真实 MapperProxy，mock 抛 "Unable to retrieve the mapperInterface and sqlSession properties" → service 聚合逻辑下沉纯函数承载单测，SQL 获取段留集成测试覆盖
- **jacoco 单类门禁会顶出测试缺口**：Task 5 过滤分支无单测 → Task 12 门禁拦（0.67<0.80）→ 补 3 用例到 100%（主代码零改动）
- **concern⑥ 定案**：局部检索查询向量=originalQuery（图输入含 caption 语境，spec §3.3 用户原问题为准；纯图片走 chat/unknown 不检索、纯文档无前缀，混合场景 caption 是有效附件语境）——不改 Task 9 worker
- **最终审查 Finding 1/2**（合并前必修，已修）：①混文件上传孤儿对象→两阶段校验（先全量类型+大小校验再落盘）；②系统检索空时 user-document 不注入→合并点提前至 chunks.isEmpty() 之前（系统空+附件 hasAny 时以 `<document>` shell 注入，spec §5.4 合并注入语义）

## 3. 进行中 / 未完成 (Pending)

- **计划 4/5（偏好记忆）**：未开始。设计决策已定（见记忆索引 s1-preference-episodic-memory-design）。下一步：用 superpowers:writing-plans 以 spec §7 生成计划 → 保存 `docs/superpowers/plans/2026-08-19-s1-plan4-preference-memory.md` → SDD 账本 `.superpowers/sdd/<plan-basename>/` → 交用户审批（重大交付物需用户审批）
- **计划 5/5（经历记忆）**：未开始（依赖 4/5，spec §8）
- **dev 手动验证附件链路**：计划 3/5 的 dev 端到端手动验证标注为待办（上传→caption→文档局部检索→user-document 注入→后续轮次重建），可并入计划 4/5 验证时一并做
- **既定待办（非本计划引入，见进度文档 §2.3 上一会话）**：BUG-8 多实例部署（待批准）、前端两目录（等启动信号）、DOCX 图片路由验证（计划 1 遗留）、BM25 检索恢复（TASK.md §4 搁置）

## 4. 环境与依赖状态 (Environment)

- 与上一会话一致：JDK 17 + Maven 3.9.16（仓库 D:/code/envs/maven/3.9.16/repo）+ Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + milvus-sdk 2.6.11 + Tika 2.9.2 + Caffeine + Gson 2.13.2（现 pom 显式声明）
- **模型通道唯一权威**：chat 统一 OpenAI 兼容端口（DashScope compatible-mode）；new chat 调用一律 OpenAiChatOptions
- **dev 启动**：`export PG_PORT=5433 REDIS_PORT=6380 && mvn.cmd spring-boot:run`（宿主机 5432/6379 被 mindsoar-* 占用）
- **构建验证**：`cd backend && mvn.cmd clean verify`（859 测试；Windows 下 spotless:apply 转 CRLF 接受）

## 5. 全局约定 (Locked Conventions，延续)

- **SDD 流程固定**：writing-plans → 用户审批 → SDD 子代理逐任务派发（实现子代理+任务审查+最多 5 修复轮+终审）；账本在 `.superpowers/sdd/<plan-basename>/`（git-ignored）；脚本在仓库根执行
- **Git 纪律**：只 add 任务文件（禁 git add -A）；docs/ 审查/计划/进度文档不提交；push 走 HTTPS `--no-verify` 加速重试
- **工程宪法**（AGENTS.md 全文）：中文注释/日志；禁全路径类名；@RequiredArgsConstructor + private final；禁循环依赖；本 service 主表 this.lambdaQuery()/lambdaUpdate()；先写 DB 后失效缓存；死代码零容忍；测试与实现同提交
- **附件链路定案**：附件不进系统知识库；双存 chat_run+chat_message；限额（图10/文档50/10个/合计100MB）；局部检索纯向量；caption 走 OpenAI 兼容通道（无 multiModel）
- **本周新增教训**（写进 memory）：Spring 双构造器 @Autowired；MP lambdaQuery 不可 mock、聚合逻辑纯函数化；jacoco 单类门禁会顶出测试缺口（补过滤分支/mime 用例）；V9 别建 JSONB（MP String 绑定失败走 TEXT 惯例）
