# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。
> 本会话上下文：上份交接文档 `docs/progress/2026-08-16-审查修复与契约定稿.md`（HEAD 1ddca94）。
> **本会话 = 执行 §2.5 下一批修复（3bdd17f，46 文件）+ pre-commit pre-push 阻断修复（12507b6），`mvn clean verify` 全门禁绿（736 测试），已 push（远程 79c7c39 → 12507b6，101 提交全量到达）。**
> 变更要点：DTO 归位根 dto/ 完成（controller/dto/ 目录已消失）；测试侧 @MockBean 清零；origin push URL 切 HTTPS（GitHub SSH 22 大流量间歇 reset）；pre-push 阶段现只跑 Maven 验证。

## 元信息 (Meta)

- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-16T21:50:00+08:00
- 版本控制提交号: 12507b6（已 push，远程 origin/main = 12507b6）
- 任务一句话描述: **执行进度文档 §2.5 下一批修复（DTO 归位 / IntegrationTestBase 死代码核验 / @MockBean 清零）并提交 push；修复 push 链路上发现的 pre-push hook 阻断与 GitHub 网络问题**

## 1. 已完成 (Completed) — 简要标记

- [x] **DTO 归位根 dto/ 包（3bdd17f，46 文件）**：15 个请求 DTO + ApiResponse/PageResponse/LoginResponse + 测试侧 ApiResponseTest 共 16 文件 `git mv` 至根 `dto/`（controller/dto/ 目录已消失）；45 文件 import/package 同步 + spotless 重排；全仓 `controller.dto` 零残留（含测试、非 Java 文件仅历史 docs 提及）
- [x] **IntegrationTestBase 死代码——核验零动作**：`git log -S assertStatus` 实锤 `assertStatus/awaitStatus` 已于 458967c（fast-follow 清理）删除，当前文件仅存被 ChatFlowIntegrationTest 使用的 `awaitTerminalStatus`（保留）
- [x] **@MockBean 残留清零（同 3bdd17f）**：AdminUserControllerSecurityTest 4 处 → `@MockitoBean`（3 处默认 + 1 处 `@MockitoBean(answers = Answers.RETURNS_DEEP_STUBS)`），javap 实锤 spring-test 6.2.14 属性名为 **`answers`**（复数）；单类 3 用例 401/403/200 全过；`grep -rn "@MockBean" backend/src/test/` 残留为 0
- [x] **全门禁验证**：`mvn.cmd clean verify` 736 测试 0 失败 0 跳过；spotless 280 文件 clean；checkstyle 0 violations；spotbugs BugInstance 0；jacoco 169 类全覆盖达标
- [x] **pre-commit pre-push 阻断修复（12507b6）**：`.pre-commit-config.yaml` 5 个通用文件检查 hooks 补 `stages: [pre-commit]`（原未限定 stages，pre-push 时全仓扫描历史 docs 尾随空白 → 自动修复与 stash 恢复冲突 → push 被阻断）；pre-push 阶段现只跑 mvn verify（与配置注释本意一致）
- [x] **push 完成**：origin push URL 切 HTTPS（`git remote set-url --push`，fetch 仍 SSH）；远程 79c7c39 → 12507b6（101 提交全量到达）；`git push --set-upstream origin main` 已设 tracking
- [x] 提交纪律保持：2 份审查报告（docs/bugs/2026-08-16-24小时变更审查-待修复bug清单.md、docs/pref/2026-08-16-性能优化建议.md）仍为未跟踪输入物，未纳入任何提交

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 D1 S1 多模态 RAG 主任务（唯一立项待办，等用户批准启动）

- 目标: 按 S1 spec（docs/superpowers/specs/2026-08-12-multimodal-rag-design.md，2026-08-14 终版）实施多模态 RAG
- 当前状态: P0-P3 bug 修复 + 宪法合规 + 覆盖率达标 + 24h 审查修复 + §2.5 下一批修复全部完成；**既定下一步 = superpowers:writing-plans 以 S1 spec 生成实施计划 → SDD**
- 涉及文件/产物: `docs/superpowers/specs/2026-08-12-multimodal-rag-design.md` [修改-已终版]
- 已锁定决策: 见记忆索引（s1-metadata-filter-design/s1-query-understanding-design/s1-prompt-design/s1-document-assembly-saa/s1-etl-multimodal-decisions/s1-user-attachment-design/s1-preference-episodic-memory-design/s1-model-selection）
- 阻塞点/风险: 无（与已完成的修复轮无依赖冲突）；实施前需用户确认计划
- **下一步具体动作**: 用户批准启动后，用 superpowers:writing-plans 以 S1 spec 生成实施计划 → 用户审批 → SDD 分任务执行（首个任务完成后 `cd backend && mvn.cmd verify` 全绿）
- 验证方式: 计划文档产出且用户批准；首个 SDD 任务完成并 verify 全绿

### 2.2 BUG-8 多实例部署（待用户批准立项，已记 TASK.md 第 1 节）

- 目标: 多实例部署时雪花 ID 不冲突（worker-id/datacenter-id 分配或配置化）+ 本地缓存一致性方案
- 当前状态: 仅记录待办，**未实现**；用户 2026-08-16 明确指示「后续扩展，吸纳不要急着实现，等我批准才能动」
- 涉及文件/产物: `TASK.md` [修改-第 1 节待办]，`backend/src/main/resources/application.yml` [待创建-方案落地时]
- 已锁定决策: 批准前不动代码；方案候选=Redis/DB 分配 worker-id 或配置化；缓存候选=Redis 分布式缓存或接受 TTL 上界
- 阻塞点: 用户批准
- **下一步具体动作**: 用户批准后按 TASK.md 接入清单立项（先出方案给用户审，再实现）
- 验证方式: 方案获批；实现后双实例联调主键无冲突

### 2.3 M-4 异步化（待管理端界面设计时一并定，已给结论不建议现在做）

- 目标: 分片内容更新/批量标注不再阻塞 HTTP 请求线程（如需）
- 当前状态: 已核对并答复用户——**不建议现在做**（批量化已解决主要痛点；异步化需新增分片级向量状态字段+轮询接口，属契约设计）；管理端界面未设计
- 涉及文件/产物: `service/impl/DocumentChunkServiceImpl.java` [修改-已做批量化，未做异步化]
- 已锁定决策: 不引入分片级状态字段；等管理端界面设计时一起定
- **下一步具体动作**: 管理端界面设计启动时，把「分片向量重建状态 + 轮询接口」纳入契约设计一并评审
- 验证方式: 契约文档增补该接口设计

### 2.4 前端（frontend/ 与 student-frontend/ 均为空目录，未设计）

- 目标: 管理端 + C 端前端实现（契约基准已定稿）
- 当前状态: 用户 2026-08-16 指示「前端还没有设计，需要等后端写的差不多才写」；后端契约基准 = `docs/contracts/2026-08-16-接口契约定稿.md` [新建-已提交]
- 已锁定决策: 错误响应体统一 ApiResponse{code,message,data}（code=HTTP 状态码，409 并发 Run/503 文案见契约文档）；集合字段恒 []；可空对象字段恒 null 不缺 key；token_count 0=未统计
- 阻塞点: 后端剩余工作 + 用户启动信号
- **下一步具体动作**: 待用户指示；AGENTS.md 强制「前端 AI 对话/消息渲染/会话界面落地前必须与用户沟通」
- 验证方式: 前端按契约文档实现后联调

### 2.5 已答复的核对项（无需动作，仅记录结论防重复审查）

- `IChatSessionService.findActiveSessions` 无调用方：用户 2026-08-16 指示**暂留**，不列入修复
- L-1（disableUser 批量黑名单）/L-6（反馈 upsert 单 SQL）/L-10（空结果不缓存）/L-14（多实例缓存）/M-7 阶段超时：已答复用户——收益低或需专项设计，维持不动
- IntegrationTestBase assertStatus/awaitStatus 死代码：**已核验提前完成（458967c 已删）**，勿重复审查

## 3. 环境与依赖状态 (Environment)

- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17）、Maven 3.9.16（D:\code\envs\maven\3.9.16，settings.xml 指定本地仓库 D:/code/envs/maven/3.9.16/repo）；Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + MapStruct 1.6.3 + Testcontainers（postgresql/redis）；pre-commit（Python 3.14.6）
- **Docker 需手动启动**：集成测试（Auth/ChatFlow/Security 集成 + 5 个 mapper XML 测试 + 冒烟）用 Testcontainers 单例容器，Docker Desktop 未启动则这些用例失败；开发联调另需 `docker compose -f docker-compose.dev.yml up -d`
- **GitHub 网络（重要环境事实）**：SSH 22 与 HTTPS 大流量传输均间歇性 reset（curl 55/56，小请求正常，1.17 MB 增量也传不完），多轮重试可碰上稳定窗口；**origin push URL 已切 https**（`git remote set-url --push origin https://github.com/mwrforever/course-rag.git`，fetch 仍为 ssh git@github.com:mwrforever/course-rag.git）；push 时若 hook 已全过可用 `git push --no-verify` 跳过 pre-push Maven 验证加速重试；`git config http.postBuffer 524288000` 已设置
- 关键配置: `backend/src/test/resources/application-test.yml`（集成测试专用，dummy dashscope key）；`backend/.env` 只含 DASHSCOPE_API_KEY（gitignore）；新配置 `auth.strict-secret`（默认 false）、`etl.embedding-batch-size: 16`、`worker.run-pool.stale-run-timeout-minutes: 10`
- 安装/构建/启动命令:
  ```bash
  cd backend && mvn.cmd clean verify        # 全门禁（spotless+checkstyle+spotbugs+jacoco BUNDLE 0.80+单类 0.80；当前全绿 736）
  cd backend && mvn.cmd spotless:apply      # 格式化（Windows 会转 CRLF；改代码后先 apply 再 verify）
  cd backend && mvn.cmd test                # 仅测试（736 用例）
  git push --no-verify origin main          # 跳过 pre-push hook 直接推（hook 已全过时）
  ```
- 常用实证/坑: ①Git Bash 管道中 grep/find 输出路径会被 MSYS 转换（`/`→`\`，xargs 传 sed 读不到文件）——批量替换文件用 python 脚本（`read_text/write_text` 传 `newline=''` 保留 CRLF 行尾）；②替换 import 时 package 声明（`...dto;` 无尾随点）与 import（`...dto.X;` 带尾随点）需分别匹配；③`@MockitoBean` 属性名是 `answers` 复数（spring-test 6.2.14 javap 实锤），`@MockBean` 是 `answer` 单数；④spotless 的 import 排序：批量改 import 后必须 `mvn spotless:apply`（新包名在字母序中位置变化）；⑤pre-push 阶段通用文件检查 hooks 若未限定 stages 会全仓扫描历史文件并自动修复（与 stash 冲突阻断 push）——已修，勿回退；⑥`git mv` 目标目录必须已存在（先 mkdir -p）；⑦spotless:apply 在 Windows 把改过的文件转 CRLF（check 接受；python 改文件需按 \r\n 匹配）

## 4. 全局约定 (Locked Conventions)

- 继承前两份交接文档全部约定（分层依赖/对象通信/MapStruct/Service 接口化/BizException/config-properties-record-constants 收编/主表 this.lambdaQuery()/按需取列/禁手动 IdWorker/缓存先写 DB 后失效/private final+@RequiredArgsConstructor/禁循环依赖/禁用弃用 API/注释日志全中文/查询必带分页/jacoco 单类 ≥80% 豁免 8 项/Testcontainers 单例容器模式）
- **新增——DTO 全在根 dto/ 包**：controller/dto/ 目录已废弃（16 文件全部归位根 dto/，含测试 ApiResponseTest）；controller 入参 DTO 一律根 dto/、出参 VO 根 vo/，禁止再建 controller 子包
- **新增——测试侧 mock 用 @MockitoBean**（org.springframework.test.context.bean.override.mockito.MockitoBean）：@MockBean 残留已清零，新测试禁止引入；深桩场景用 `@MockitoBean(answers = Answers.RETURNS_DEEP_STUBS)`
- **新增——pre-push 阶段只跑 Maven 验证**：.pre-commit-config.yaml 通用文件检查 hooks 均已限定 `stages: [pre-commit]`；pre-push 若再发现文件检查类失败，先确认配置未被回退
- **新增——push 走 HTTPS**：origin push URL = https://github.com/mwrforever/course-rag.git（fetch 仍 SSH）；SSH 22 大流量不稳，勿改回
- **新增——接口契约**（docs/contracts/2026-08-16-接口契约定稿.md，前端实现基准）：错误响应体统一 `ApiResponse{code,message,data}`，code 与 HTTP 状态码同值（409=并发 Run「会话 X 已有活跃的 Run，无法创建新 Run」、503=DB 不可用/消息队列不可用/文档解析队列繁忙）；集合字段恒输出 `[]` 不输出 null；可空对象字段恒输出 null 字段不缺 key；chat_message.token_count 语义 0=未统计
- **新增——BUG-7 部署约定**：生产必须设 `auth.strict-secret: true` 并配置 JWT_SECRET；开发保持 false（内置默认密钥仅限开发）
- **新增——BUG-8 待批准**：多实例部署事项（TASK.md 第 1 节）未经用户批准不得实现
- **新增——H-1 投递模型**：SSE 事件投递走每 run 有界队列+独立投递线程（推送与生成解耦）；回放批次锁内入队保证顺序；队列满摘除订阅者依赖 EventSource 自动重连+ring 回放补偿——后续改动不得把 send 移回生成线程
- git 纪律（只 add 任务文件，禁 git add -A）；docs/ 下审查报告等输入物文件不纳入提交；superpowers 流程固定（核验→spec 审批→plan 审批→SDD）；MapStruct/Lombok 配置变更后必须 `mvn clean` 重编译
