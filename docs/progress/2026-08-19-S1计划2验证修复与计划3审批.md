# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)

- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-19T00:30:00+08:00
- 版本控制提交号: 30f5738（S1 计划 2/5 手动验证 4 bug 修复全量推送 origin/main）
- 任务一句话描述: **S1 计划 2/5 开发环境手动验证完成（4 bug 修复已推送）+ 计划 3/5（附件）已写好待审批 + 用户两项定案（附件纯向量 / BM25 搁置入 TASK.md）**

## 1. 已完成 (Completed) — 简要标记

- [x] **推送确认**：S1 计划 2/5 的 15 commits（87f75f1..95696e7）已在远程（上会话已推），远程 HEAD 与本地一致
- [x] **dev 环境手动验证全链路**（详见 docs/progress/2026-08-18-S1计划2开发环境手动验证与修复.md）：ETL（解析/分片/去重/INDEXED/级联软删）、图片 caption（qwen3.7-flash）、检索链路（QU→CourseNameMapper→检索→document 注入→回答标注"资料 [1]"）、chat/unknown 意图分支
- [x] **Bug-1 修复**（9fb49b2）：Tika 2.9.2 图片提取从未生效（extractInlineImages 默认 false 字节码实锤）→ EtlPipeline 显式开启 + RAW_IMAGES + PdfImageExtractionTest（真实 PDF fixture）
- [x] **Bug-2 修复**（9fb49b2）：caption 走 text-generation 接口 url error（SAA multiModel 路由）→ ImageCaptionService 加 multiModel(true)（**注：后续 Bug-4 通道切换后该开关已不需要，见 30f5738**）
- [x] **Bug-3 降级修复**（812dd42）：milvus-sdk-java EmbeddedText 与 BM25 Function 不兼容（issue #1402 Open，服务端 INTERNAL 后 SDK 无限重试 200s）→ SearchKnowledgeTool 加 `retrieval.sparse-enabled` 开关（默认 false，dense-only 混合检索 1.6s 正常）
- [x] **Bug-4 解决**（30f5738，用户裁决"统一 openai 协议兼容端口"）：qwen3.x 新模型原生 text-generation 接口 url error 实测实锤 → chat 全通道切 Spring AI OpenAI ChatModel（base-url=https://dashscope.aliyuncs.com/compatible-mode，spring.ai.model.chat=openai 停用 DashScope chat 双 bean；embedding/rerank 保留 SAA 原生）；主对话/QU/摘要/caption 四处 DashScopeChatOptions→OpenAiChatOptions；**dev 启动不再需要模型覆盖环境变量**
- [x] **验证报告落盘**：docs/progress/2026-08-18-S1计划2开发环境手动验证与修复.md（含 Bug-4 解决方案更新）
- [x] **计划 3/5 已写好**：docs/superpowers/plans/2026-08-18-s1-plan3-user-attachments.md（12 任务）
- [x] **用户两项定案**：① 附件局部检索保持纯向量（计划 3/5 Task 7 内存余弦不变）；② BM25 恢复暂时搁置，已存 TASK.md §4 后置处理
- [x] 全量门禁：805 测试 + spotless + checkstyle + spotbugs + jacoco 全绿（clean verify BUILD SUCCESS），3 commits（9fb49b2/812dd42/30f5738）已推送

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 计划 3/5（用户附件会话级处理）审批 + SDD 执行

- 目标: S1 计划 3/5 落地（用户上传图片/文档附件的会话级局部处理：上传端点、VLM caption、文档局部检索、attachments_json 双存、user-document 合并注入），完成后 dev 手动验证
- 当前状态: 计划文档已写好待用户审批（docs/superpowers/plans/2026-08-18-s1-plan3-user-attachments.md，12 任务：V9 迁移→限额配置→上传端点→下载/Caffeine 缓存→图片 caption→文档局部处理→局部检索→入队/双存落库→QU caption 拼装→user-document 合并→后续轮次重建→收尾）；**未开始执行**
- 涉及文件/产物:
  - `docs/superpowers/plans/2026-08-18-s1-plan3-user-attachments.md` [新建] — 计划全文，含每个任务的测试/实现代码
  - `docs/progress/2026-08-19-S1计划2验证修复与计划3审批.md` [新建] — 本交接文档
- 已锁定决策:
  - 附件不进系统知识库（不写 document_chunk/Milvus）；字节 hash 只做内存级 Caffeine 唯一维护
  - 存储三表：chat_run + chat_message 各新增 attachments_json（V9 迁移），state/checkpoint 不存；后续轮次以 chat_run 为入口重建
  - 限额（用户拍板）：单图 ≤10MB、单文档 ≤50MB、单次 ≤10 个、合计 ≤100MB（attachment 配置段）
  - 图片统一 VLM caption（qwen3.7-flash），标注"图片1/2"序号；caption 就绪后拼入 QU {query}
  - 文档首版仅文本类（PDF/Word/TXT/MD），不做内嵌图片提取；文档内容不参与系统检索查询
  - **附件局部检索保持纯向量（用户 2026-08-18 拍板）**——Task 7 内存余弦 Top-K，不加 BM25/关键词路
  - document 块：<system-document> 与 <user-document> 子标签分离，经既有 config.metadata() → DocumentAssemblerInterceptor 通道（不重复实现）
  - 模型：caption 用 qwen3.7-flash 走 OpenAI 兼容通道（OpenAiChatOptions，无需 multiModel 开关——Bug-4 切换后已不需要）
- 阻塞点/风险: 无代码阻塞；需用户审批计划文档后才能进入 SDD
- **下一步具体动作**: 向用户提交计划 3/5 审批 → 获批后按 SDD 流程逐任务派发（新账本 `.superpowers/sdd/2026-08-18-s1-plan3-user-attachments/`，脚本须在仓库根目录执行）
- 验证方式: 计划获批 + 每任务 verify 全绿 + 全量 805+ 测试 + dev 手动验证附件链路

### 2.2 计划 4/5（偏好记忆）与 5/5（经历记忆）

- 目标: S1 spec §7（偏好记忆）/§8（经历记忆）落地
- 当前状态: 未开始（设计决策已定，见记忆索引 s1-preference-episodic-memory-design）
- 涉及文件/产物: [待创建] `docs/superpowers/plans/2026-08-19-s1-plan4-preference-memory.md` 等
- 已锁定决策: 偏好 write_score=0.4e+0.4s+0.2c、晋升 count≥5 且 write_score≥0.75、独立 PreferenceInterceptor（<preference> HumanMessage 最前、Caffeine 30min 冻结、token 2000/guaranteed 500）；经历三维打分无 stability；预算 1200；PG user_episodic_memory + Milvus memory_chunks（user_id 硬隔离）；4/5 的 PreferenceInterceptor 可参考 2/5 的 DocumentAssemblerInterceptor 先例——注意 2/5 终审「document 每轮重注」决策对偏好注入有参考价值，写入计划时需明确偏好块是否每轮注入
- 阻塞点/风险: 依赖计划 3/5 完成（消费其附件上下文通道与 Caffeine 基建）；偏好/经历提取涉及真实模型调用需 dev 验证
- **下一步具体动作**: 计划 3/5 审批后，用 superpowers:writing-plans 以 spec §7 生成计划 4/5 → 保存 docs/superpowers/plans/2026-08-19-s1-plan4-*.md → 交用户审批
- 验证方式: 计划获批 + 每计划 verify 全绿

### 2.3 既定待办（非本次会话引入）

- **BUG-8 多实例部署**（TASK.md §1）：未经用户批准不得实现
- **前端两目录**（frontend/ 与 student-frontend/ 均空）：等用户启动信号；AI 对话/消息渲染/会话界面落地前必须与用户沟通；管理端界面按生产标准直接落地
- **DOCX 图片路由验证**（计划 1 决策点 4 风险）：上传 DOCX 验证 Tika 图片提取路由（TikaImageExtractor 对 OOXML 内嵌图片的捕获），本次手动验证无 DOCX fixture 未覆盖
- **BM25 检索恢复**（TASK.md §4）：已搁置（用户 2026-08-18 拍板），恢复时从两条候选方案选一（等 SDK 修复 issue #1402 后 retrieval.sparse-enabled=true 一行还原 / 应用侧 BM25 向量 + collection 重建）

## 3. 环境与依赖状态 (Environment)

- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17）、Maven 3.9.16（本地仓库 D:/code/envs/maven/3.9.16/repo）、Spring Boot 3.5.8 + Spring AI 1.1.2（chat 走 spring-ai-starter-model-openai）+ SAA 1.1.2.0（embedding/rerank/Graph）+ MyBatis-Plus 3.5.12 + milvus-sdk-java 2.6.11 + Tika 2.9.2 + Testcontainers；Docker 29.6.2 运行中
- 关键配置:
  - 模型通道（2026-08-18 切换定稿）：`spring.ai.model.chat=openai`、`spring.ai.model.embedding=dashscope`、`spring.ai.model.rerank=dashscope`；openai base-url=`https://dashscope.aliyuncs.com/compatible-mode`（**不带 /v1**，Spring AI 自动补）；主对话 qwen3.8-max / QU qwen3.7-flash / caption qwen3.7-flash / embedding qwen3.7-text-embedding / rerank qwen3-rerank
  - **dev 启动无需模型覆盖环境变量**（Bug-4 已修复）；但 PG/Redis 端口仍用 5433/6380（宿主机 5432/6379 被另一项目 mindsoar-* 容器占用，Up 中——mindsoar 停止后可还原默认端口）
  - `auth.strict-secret` 默认 false；测试配置 `backend/src/test/resources/application-test.yml`（含 openai dummy key + 不可达 base-url，模型 bean 由 @MockitoBean 替换）
- 安装/构建/启动命令:
  ```bash
  cd D:\code\project\commerce-customer\commerce-customer\backend
  mvn.cmd clean verify        # 全门禁（当前 805 测试全绿）
  mvn.cmd test -Dtest=XxxTest # 单类
  mvn.cmd spotless:apply      # 格式化（Windows 转 CRLF，check 接受）
  # dev 启动（端口覆盖 + 默认模型通道）：
  docker compose -f docker-compose.dev.yml up -d   # 若端口冲突需重建 docker-compose.dev.local.yml 覆盖 5433/6380
  export PG_PORT=5433 REDIS_PORT=6380 && mvn.cmd spring-boot:run
  # 本地辅助：appdata/test/ 下有测试 PDF 生成脚本（gen_test_pdf.py，reportlab+中文字体）与上传脚本（git-ignored）
  ```

## 4. 全局约定 (Locked Conventions)

- **SDD 流程固定**：writing-plans 生成计划 → 用户审批 → SDD 子代理逐任务派发（每任务 = 实现子代理 + 任务审查 + 最多 5 修复轮 + 终审）；账本在仓库根 `.superpowers/sdd/<plan-basename>/progress.md`（git-ignored）；脚本须在仓库根目录执行；计划 3/5 新账本 `.superpowers/sdd/2026-08-18-s1-plan3-user-attachments/`
- **模型通道（2026-08-18 用户裁决，不得推翻）**：chat 统一走 OpenAI 协议兼容端口（DashScope compatible-mode），DashScopeChatModel/SAA 原生 chat 已停用；embedding/rerank 保留 SAA DashScope 原生；新接入的 chat 调用一律 OpenAiChatOptions（`.model()` 非 `.withModel()`）
- **计划 2/5 关键决策（不得推翻）**：条件边路由键 = IntentType.code() 小写（knowledge_question/chat/unknown）；document 每轮模型调用重注（去幂等，偏离 spec §3.3，javadoc 已标注）；queryPlan 每 run 覆盖重写（ReplaceStrategy）；`{context}`/`{query}` 占位符无 $；rewrittenQueries state key 已全量退役
- **Bug-3 降级开关**：`retrieval.sparse-enabled=false`（milvus-sdk-java EmbeddedText bug 未修复前不得置 true；恢复走 TASK.md §4 两条候选之一）
- **附件链路定案**：局部检索纯向量（用户 2026-08-18 拍板，计划 3/5 Task 7 内存余弦）
- **Git 纪律**：只 add 任务文件、禁 git add -A；docs/ 下审查报告与计划/进度文档不纳入提交；push 走 HTTPS（origin push URL 已切 https，fetch 仍 ssh），hook 全过后 `git push --no-verify` 加速重试；GitHub 网络不稳需多次重试
- **工程宪法**（AGENTS.md 全文）：注释/日志中文；禁全路径类名（javadoc {@link} 除外）；@RequiredArgsConstructor + private final；禁循环依赖；禁弃用 API；本 service 主表 this.lambdaQuery() 链式、wrapper 限非主表；先写 DB 后失效缓存；死代码零容忍（本次改动产生的废弃配置/分支/测试同提交清理）；测试与实现同提交；新测试必须真实覆盖目标逻辑（计划 2 曾在审查中抓到假测试，新计划须以此为戒）
- **环境事实**：宿主机 5432/6379 被 mindsoar-* 容器占用（另一项目，勿停）；开发库无业务数据可 drop 重建（用户已拍板）；GitHub SSH22/HTTPS 大流量间歇 reset，origin push 走 HTTPS
