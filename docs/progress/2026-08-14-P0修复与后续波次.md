# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-15T14:30:00+08:00
- 版本控制提交号: 7576ed0（P2/P3 波最终提交，含最终审查修复波；P1 波次 2375b09c 在前）
- 任务一句话描述: bug 总清单三波修复全部完成（254→257 全量测试 + 真实环境验证）；用户 2026-08-15 晚指示：将 P2/P3 波新发现写入 bug 清单（已写），新会话修正新发现项后进入 S1 主任务

## 1. 已完成 (Completed) — 简要标记
- [x] **基线清理（S2/S3 认证与 Tab 语义）**: 9 提交 abce20a..de74a5e，全量测试 186/186；S6 核验已修零动作、S5 纯新增移出
- [x] **bug 总清单全量核验**: 30 修复点分域核验（4 并行子 agent + 主 agent 抽查），P1-1/P3-1 已修、其余全部实锤存在
- [x] **第一波 P0（4 项 15 子项）**: 10 提交 9480b8c..5969fb7，全量测试 219/219；权限桥接与越权修复同波闭环；最终审查拦截的 fail-open 缺口已 fail-closed
- [x] **第二波 P1（3 项 + 5 子项）**: 9 提交 908339b..2375b09c，全量测试 231/231；SDD 逐任务 TDD + 两级审查 + 最终整体审查 Approve；真实环境验证完成（P1-3 取消+回滚+重跑落库 / P1-4 reparse 旧向量清除 / KB 删除三端清理 / upload uuid 路径实证）
- [x] **P0 spec 与计划**: `docs/superpowers/specs/2026-08-14-p0-security-fixes.md` + `docs/superpowers/plans/2026-08-14-p0-security-fixes.md`（用户已批准）
- [x] **P1 spec 与计划**: `docs/superpowers/specs/2026-08-14-p1-storage-sse-fixes.md` + `docs/superpowers/plans/2026-08-14-p1-storage-sse-fixes.md`（用户 2026-08-15 批准；含用户裁决：upload 改 uuid 先行 {kbId}/{uuid}.{ext} + 单向补偿，替代原双向补偿）
- [x] **总清单修复状态表**: `docs/bugs/2026-08-14-待修复bug总清单.md` 顶部已加三波修复状态

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 第二波 P1 —— 已完成（2026-08-15）
- 结果: 9 提交 908339b..2375b09c（SDD 逐任务 TDD + 两级审查 + 最终整体审查 Approve），全量测试 231/231
- 修复内容: P1-2（终态判定补发 end + replayAndSubscribe 原子化，删死代码 replay/replayFrom）、P1-3（容器级浅拷贝保 Message 类型，删 deepCopyState）、P1-4 五子项（Milvus filter 直删 + ByCourseId / 课程同步 Milvus / MinIO 删除上抛 + 调序 / KB 级联删对象 / upload uuid 先行 {kbId}/{uuid}.{ext} + 单向补偿）
- 真实环境验证（2026-08-15 完成，docker 基础设施 + 真实 LLM）:
  - P1-3: 第 2 轮对话取消 → 回滚 checkpoint 成功（old→new checkpointId，无异常）→ 后续轮次消息完整落库 + 工具调用正常（类型保留实证）
  - P1-4: reparse 后 Milvus 旧 chunk_id 0 行（修复前会双份残留）；删知识库后 MinIO 对象 0 残留 + Milvus 0 行 + PG 软删；上传 sourcePath 为 uuid 格式实证
- **真实环境发现的环境问题（仓库既有，非 P1 引入，建议下一波立项修复）**:
  1. V6 迁移 TIMESTAMPTZ vs 实体 LocalDateTime——PG JDBC 42.7.8 下任何查询必炸（验证时已就地 ALTER 开发库绕过，未改仓库文件）
  2. jsonb vs varchar 插入不匹配（chat_run.meta_json / course_info.tags 等，已就地 ALTER 绕过）
  3. 种子 admin 密码 hash 与 V6 注释不符（注释 admin123 实际不匹配，已就地 UPDATE 绕过）

### 2.2 第三波 P2/P3 立项与修复（ETL 幂等 / 跨端契约 / 观察项低成本子集 + 403 契约统一）—— 已完成（2026-08-15）

- 结果: 9 提交 7042265..b1515ac（SDD 逐任务 TDD + 两级审查 + 全部 Approve），全量测试 254/254
- 修复内容:
  - P2-1: ETL 状态守卫（条件更新原子抢占 PENDING/FAILED→PARSING）+ 部分失败标 FAILED + try-with-resources 关流；上传文件类型白名单（pdf/docx/pptx/md/txt）+ 大小校验引用 maxFileSizeMb（修复死配置）
  - P2-2: 分片端点契约对齐（PUT /chunks/{id}、POST /batch-corrected）；文档列表 status/q/sort 筛选（Wrappers 链式 + TEACHER 过滤内联）；dashboard 统计三端点（AdminDashboardController + DashboardService，分组统计走 mapper XML——新建 UserFeedbackMapper.xml 基建）
  - P2-3: GlobalExceptionHandler 真实 HTTP 状态码（ResponseStatusException→ResponseEntity 动态 status、IAE→400、SecurityException→403、Exception→500；AccessDenied 403 保持）
  - P3: 死代码清理（runSnapshot/toSseText/findActiveRun+cancelRun/AuthConfig public 排除）；A11 RT 一次性旋转原子化（mark_rt_used.lua 单条脚本消除 TOCTOU，Redis 异常降级放行 + PG 黑名单兜底）
  - 环境问题三项: V6 迁移 TIMESTAMPTZ 29→TIMESTAMP、JSONB 5→TEXT、删 gin 索引、种子 admin hash 修正（真 admin123 hash）
- 真实环境验证（2026-08-15 完成，drop 重建 + Flyway V6/V7 重跑成功 + 真实 LLM）:
  - 登录 admin/admin123 成功（种子 hash 修正生效）；TIMESTAMP 列正常读取（环境-1 修复实证）；metadataJson 正常（环境-2 修复实证）
  - 上传 .txt → ETL → INDEXED（2 chunks，uuid 路径）；上传 .exe → 业务 400 JSON（白名单 + 真实 HTTP 400）
  - PUT /chunks/{id} → 200；PUT 不存在 id → 400 非 405；POST /batch-corrected → 200
  - dashboard 三端点 200 + 口径正确（trend 0 补位升序日期正确，XML 分组 SQL 实证）；文档列表 status/q/sort 筛选生效
  - 403 真实状态码（admin 访问 student 端点 → HTTP 403 + 无权操作）；401 未认证；ResponseStatusException 动态状态实证
- **真实环境新发现遗留（既有问题，非本波回归，建议后续波次）**:
  1. GET /documents?page=abc → HTTP 500（MethodArgumentTypeMismatchException 无专门 handler；登录空 body → 500 同因——Spring MVC 参数校验异常族未覆盖，错误契约后续扩展点）
  2. POST /knowledge-bases {"name":""} → 200 建库成功（name 无非空校验，既有行为）
  3. GET /chunks/{id} 不存在 → HTTP 200 + body 404（spec §3.1 已知遗留，controller 内联 ApiResponse.error）
  4. SecurityConfig:54 仍放行 /api/v1/public/**（与 AuthConfig 同一死配置的另一半，plan 范围外未删）
  5. 存量合规遗留（宪法 2026-08-15 更新后）：10 个 service 仍 @Autowired 字段注入（ChatMessageService/ChatSessionService/ChatRunService/UserFeedbackService/DocumentChunkService/CourseService/CourseScheduleService/KnowledgeBaseService/DocumentService/EnrollmentService）；CourseService/EnrollmentService/KnowledgeBaseService/AuthSessionService 等大量 `new LambdaQueryWrapper`；DocumentService.update/delete/reparse 的 new wrapper；AdminDocumentController.download 与 DeviceKickService/PromptLoader/CustomSummarizationHook/MilvusCollectionInitializer 全路径类名；ChatRunService.updateStatus new wrapper；SseEvent 英文异常消息——均历史遗留，精准修改原则未动
- **上述遗留已正式写入 bug 清单**（`docs/bugs/2026-08-15-24小时变更审查-待修复bug清单.md`）：1→P0-6 真实环境实证、2→P2-12 新增、3→与 P1-3 重合、4→P3-4 新增、5→P1-6 补充两处（AdminChunkController 未用 Logger、MybatisPlusTestHelper 全路径类名）；**修复入口见 §2.4**（用户指定新会话修正）
- 相关文件/产物:
  - `docs/superpowers/specs/2026-08-14-p2-p3-contract-etl-fixes.md` [已批准] — 本波 spec（已按 2026-08-15 宪法两轮更新调整：Wrappers 链式/XML 映射/private final+@RequiredArgsConstructor）
  - `docs/superpowers/plans/2026-08-14-p2-p3-contract-etl-fixes.md` [已批准] — 本波实施计划（10 任务，Task 1-9 已执行完毕）
  - SDD ledger: `.superpowers/sdd/2026-08-14-p2-p3-contract-etl-fixes/progress.md`（含 9 任务审查结论 + 全部 deferred minor 清单）
- **下一步具体动作**: S1 主任务（§2.3）——调 `superpowers:writing-plans` 以 S1 spec 为输入生成实施计划

### 2.4 下一波：bug 清单新发现项修正（用户 2026-08-15 晚指定，新会话执行）
- 目标: 修正 `docs/bugs/2026-08-15-24小时变更审查-待修复bug清单.md` 中 P2/P3 波次新发现/实证的 4 项（全部真实环境实证或源码实锤），全量测试通过 + 真实环境复验
- 当前状态: 清单已补充完毕（2026-08-15 晚）；**代码零改动**，待新会话按 superpowers 流程（核验 → spec → 审批 → plan → SDD）或既有 bug 波次流程修复
- 涉及文件/产物（修复方向）:
  - `backend/src/main/java/com/commerce/rag/controller/GlobalExceptionHandler.java` [修改] — **P0-6 实证项**：补 MethodArgumentTypeMismatchException / HttpMessageNotReadableException / MethodArgumentNotValidException → 400 handler（与 P2-3 真实状态码方向一致，消除"?page=abc→500、登录空 body→500"）
  - `backend/src/main/java/com/commerce/rag/dto/`（KnowledgeBaseCreateRequest）+ `AdminKnowledgeBaseController`/`KnowledgeBaseService` [修改] — **P2-12**：name 加 @NotBlank + @Valid（空 name → 400，参照 LoginRequest 惯例）
  - `backend/src/main/java/com/commerce/rag/auth/SecurityConfig.java` [修改] — **P3-4**：删除 :54 `.requestMatchers("/api/v1/public/**").permitAll()`（AuthConfig 已删的另一半）
  - `backend/src/main/java/com/commerce/rag/controller/AdminChunkController.java` [修改] — **P1-6 补充**：删未使用 Logger 字段（:45，与 DashboardService 同型已修先例）
  - `backend/src/test/java/com/commerce/rag/.../MybatisPlusTestHelper.java` [修改] — **P1-6 补充**：全路径类名 import 化
  - 若新会话选择顺带处理清单其他项（P1-3 内联 404 双轨、P0-6 相关 controller 内联 error 等），以清单 §七 修复顺序建议为准
- 已锁定决策:
  - 错误契约统一方向：客户端参数错误 → 真实 HTTP 400（延续 P2-3，禁回退 200+body code 双轨）
  - 宪法强制（AGENTS.md 2026-08-15 两轮更新）：禁止全路径类名、Wrapper 一律 Wrappers 链式、依赖注入 private final + @RequiredArgsConstructor、复杂 SQL 走 mapper XML、注释/日志全中文
  - 测试与实现同次提交；因改动失效的旧测试同提交删除
  - git 纪律：git add 只加任务文件禁 add -A；开发库无业务数据可 drop 重建
- 阻塞点/风险: 无阻塞。注意 GlobalExceptionHandler 已有 `@ExceptionHandler(Exception.class)` 兜底——新增专门 handler 后 Spring 按最具体异常匹配，互不冲突；测试要断言真实 HTTP 状态码（沿用 GlobalExceptionHandlerTest 的 @ResponseStatus 反射断言模式）
- **下一步具体动作**: 新会话打开项目后：①读 `docs/bugs/2026-08-15-24小时变更审查-待修复bug清单.md` 的 P0-6/P2-12/P3-4/P1-6 四条 + 本进度文档；②按 superpowers 流程立项（核验源码 → spec 审批 → 计划审批 → SDD 逐项 TDD）；③每项修复配单测（参数错误 400 断言、空 name 400 断言、SecurityConfig 无 public/** 断言）
- 验证方式: `cd backend && mvn.cmd test` 全过；真实环境（docker compose up + 后端 8080）：`?page=abc` → HTTP 400、登录空 body → HTTP 400、`POST /knowledge-bases {"name":""}` → HTTP 400（非 200）、grep SecurityConfig 无 public/**

### 2.3 S1 主任务（多模态 RAG 基础链路重构）
- 目标: 按 `docs/superpowers/specs/2026-08-12-multimodal-rag-design.md`（用户已批准的唯一真相源，12 章节）落地单跳多模态 RAG 全链路
- 当前状态: spec 已批准、代码零改动；**P0/P1/P2/P3 四波 bug 修复全部完成（254/254），可启动**
- 涉及文件/产物: 见 S1 spec §9 组件变更清单；DB 细节（V6 改版 DDL + Milvus 重建 schema + 新表 DDL）在 writing-plans 阶段补齐
- 已锁定决策: S1 spec 全文即锁定决策（约 30 项，见旧进度文档 `2026-08-14-多模态rag重构spec定稿.md` §2.1）
- **下一步具体动作**: 调 `superpowers:writing-plans` 以 S1 spec 为输入生成实施计划（用户审批后 SDD 执行）
- 验证方式: 实施计划无占位符；执行后 `cd backend && mvn.cmd test` 全过；图片文档 ETL 端到端（PG 有 image chunk、Milvus 可检索、SSE 返回 image_url）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17）、Maven 3.9.16（D:\code\envs\maven\3.9.16，仓库 D:\code\envs\maven\3.9.16\repo）；Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + PG 驱动 42.7.8
- 关键配置: `backend/.env` 只含 DASHSCOPE_API_KEY（gitignore）；PG/Redis/MinIO 凭证与 docker-compose.dev.yml 一致（postgres:latest / redis / pgsty/minio:latest / milvusdb/milvus:v2.6.19 / quay.io/coreos/etcd:v3.5.25 镜像均在本地）
- **环境问题三项已修复（2026-08-15 P2/P3 波 b1515ac，drop 重建后实证）**:
  1. ~~V6 迁移 TIMESTAMPTZ vs 实体 LocalDateTime（登录即 500）~~ → V6 全部改 TIMESTAMP，重建后 createdAt 正常读取（实证）
  2. ~~jsonb vs varchar 插入不匹配~~ → V6 改 TEXT（chat_run.meta_json / course_info.tags 等 5 处）+ 删 gin 索引；重建后上传/查询正常（实证）；V7 state_data JSONB 为 SAA 框架表（框架 PGobject 读写）不改
  3. ~~种子 admin 密码 hash 与注释不符~~ → V6 种子 hash 已换真 admin123 hash（`$2a$10$4Tr8GR4XD98OTopP6/vK5eYsK8yRsRPOjdYzBgK9eahMJDo6KpL8.`），重建后登录成功（实证）
- 安装/构建/启动命令:
  ```bash
  docker compose -f docker-compose.dev.yml up -d   # 基础设施
  cd backend && mvn.cmd test                        # 全量测试（当前 254/254）
  cd backend && mvn.cmd test -Dtest=XxxTest         # 单测试类
  # 真实环境验证链路（2026-08-15 P2/P3 波已验证可行）：
  #   登录 admin/admin123（V6 种子 hash 已修正）→ POST /api/v1/admin/knowledge-bases 建库
  #   → POST /api/v1/admin/documents multipart 上传（中文参数需 URL 编码，否则 Tomcat 400）
  #   → ETL → INDEXED → PUT /api/v1/admin/chunks/{id} / POST /batch-corrected
  #   → GET /api/v1/admin/dashboard/stats + feedback/stats?period + feedback/trend?days
  #   → GET /api/v1/admin/documents?status=&q=&sort= → 403/401 真实状态码验证
  ```

## 4. 全局约定 (Locked Conventions)
- **AGENTS.md 是纯工程宪法**：分层依赖/对象通信约束/目录职责/并发异步/MyBatis-Plus 规范/缓存一致性/代码规范，实现必须逐条遵守
- **superpowers 流程固定**：核验（源码实锤）→ spec（docs/superpowers/specs/）→ 用户审批 → writing-plans（docs/superpowers/plans/）→ 用户审批 → SDD（subagent-driven-development：每任务 TDD + 两级审查 + 修复循环，ledger 在 .superpowers/sdd/）
- **注释/日志/文档全中文**；测试与实现同次提交；因改动失效的旧测试直接删除；Entity 不出数据层（课程管理链路 Entity 越界重构另行决策）
- **git 纪律**：工作区有大量无关未提交改动（旧根结构删除等历史遗留），git add 只加任务明确列出的文件，禁 `git add -A`；Windows 用 `mvn.cmd`；pre-commit 的 spotless 为全模块检查，遇到 HEAD 旧版违规文件先提交遗留修复勿 SKIP 钩子；**backend/ 仅 41/167 文件被跟踪，大量文件首次入库会显示为新增（diff 粒度受限），126 文件基线提交待用户决策**
- **DB 约定**：开发阶段直接改 V6/V7 迁移 + drop 重建，不加增量迁移文件；Milvus collection 同样 drop 重建；**开发库无业务数据（用户 2026-08-15 确认），可放心 drop 重建**
- **已修复项不可回退**：P1-1 登出吊销（556c00d）、P3-1 kick PG 审计（4defa1f）、P0 全波（9480b8c..5969fb7）、P1 全波（908339b..2375b09c）、P2/P3 全波（7042265..b1515ac）
- **宪法 2026-08-15 两轮更新**：①MyBatis-Plus 规范——Wrapper 一律 Wrappers.lambdaQuery()/lambdaUpdate() 链式禁止 new、禁止业务层拼 SQL 字符串、复杂 SQL 走 mapper XML 映射文件；②Java 全局规范——禁止全路径类名（含测试）、注解优先（Lombok）、依赖注入统一 private final + @RequiredArgsConstructor（仅构造器有初始化逻辑才手写并注释）、严禁循环依赖（循环时注入 wrapper 对象解耦）；mapper 调用传值不传 wrapper；ServiceImpl 内部用 this.lambdaQuery()，跨 service 经依赖注入
- 前端（frontend/ 与 student-frontend/）为空目录未实现，两前端完全独立禁共享；前端 Agent 对话界面落地前必须与用户沟通
