# 进度文档 (Session Handoff)

> S1 计划 4/5（偏好记忆）SDD 执行完成并推送 origin/main。

## 元信息 (Meta)

- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-19（实际完成 2026-08-20）
- 版本控制提交号: 100abdb（S1 计划 4/5 全量推送 origin/main，本地=远程）
- 任务一句话描述: **S1 计划 4/5 偏好记忆 SDD 全 11 任务执行完成（14 commits 一次推送成功）+ 终审 clean（0 Critical/0 Important，必须修清单为空）+ 925 测试全门禁绿（jacoco 单类 100% / bundle 95.49%）**

## 1. 已完成 (Completed)

- [x] **计划 4/5 用户审批**（用户「批准」+ 两项决策按推荐值定稿：①软删 0/1+@TableLogic；②显式表达 explicitness≥0.8 直达 active）
- [x] **SDD 全 11 任务逐任务派发 + 任务审查**（账本 `.superpowers/sdd/2026-08-19-s1-plan4-preference-memory/progress.md`）：
  - Task 1：V11 user_preference 表（软删 0/1、单/多值 active 唯一索引）+ 实体 + Mapper + Testcontainers schema 测试（e94f626）
  - Task 2：PreferenceKeys 常量 + MemoryProperties 全配置（阈值/权重/预算/曲线零硬编码）+ MemoryConfig + yml memory 段（191fe6e + fix 801f937 去全路径类名 I-1）
  - Task 3：值对象 + memory-extraction.yml（标签式防注入）+ PreferenceExtractionService（key 白名单/value 归一化/降级）（dc9340e + fix bc325dd 边界补测 I-1/M-1）
  - Task 4：PreferenceDecisionEngine 纯规则决策引擎（write_score/stability 曲线/观察池晋升/冲突 UPDATE/多值并存/显式豁免），行覆盖 100%（4c925fc）
  - Task 5：IPreferenceService 决策执行落库（事务原子写 + DELETE 软删 + 注入读取）+ 纯函数单测 + Testcontainers 集成（aa5c040）
  - Task 6：PreferenceBlockService 偏好块组装（guaranteed 500 + 扩展 1500 截断）+ PreferenceCacheService（30min 冻结）（54e5fc2）
  - Task 7：PreferenceInterceptor（<preference> HumanMessage 前置 + metadata userId），分支 100%（b0136bd）
  - Task 8：MemoryExtractionInputAssembler + MemoryExtractionPipeline（30s 防抖独立线程池 + 超时 + 失败丢弃 + (key,value) 去重）（ac4f390 + fix 1f10ac6：@Component wiring C-1 + 超时用例 I-1）
  - Task 9：ChatRequestWorker 接入（run COMPLETED 后异步触发，error/cancel 不触发）+ 测试（0712e4e）
  - Task 10：LeadAgentGraph 注册 PreferenceInterceptor（<preference> 端到端接通）（8ed2f12）
  - Task 11：门禁收尾——SpotBugs REC_CATCH_EXCEPTION/NP_NULL 修复 + jacoco 单类补测（PreferenceServiceImpl 0.62→100%）（100abdb）
- [x] **终审（整分支 14 commits / 39 文件 / +3180）**：**可推送、必须修清单为空**；链路接缝经 SAA 源码实证（metadata userId→ModelRequest.context、system+preference 序、与 Coalescing/Document 拦截器链无冲突）；user_id 硬隔离/LLM01 注入/防注入/冻结机制/两项已批准决策全链一致
- [x] **全量门禁**：`mvn.cmd clean verify` BUILD SUCCESS——**925 测试全绿**（含 Testcontainers 集成）+ spotless/checkstyle/spotbugs 全过 + jacoco 单类 100%（PreferenceServiceImpl 117/117）/ bundle 行 95.49%
- [x] **推送**：`git push --no-verify origin main`（d0527c8..100abdb）第 6 次尝试成功（GitHub HTTPS 间歇 reset，重试收敛），本地=远程

## 2. 计划 4/5 关键工程决策与实证（供计划 5/5 参考）

- **软删口径裁决**：spec §7.2 记「deleted=时间戳」但项目全局既有约定为「deleted 0/1 + @TableLogic」——按全局约定落地（用户批准），MP 逻辑删除自动过滤、DELETE 软删保留物理行审计（集成测试用原始 SQL 验 deleted=1）
- **显式表达直达 active 豁免**：按 spec 字面（writeHigh=0.75 + stability(1)=0.25）单次全新偏好 ws 恒 ≤0.70 到不了直接写线 → 决策引擎加 「explicitness≥explicitUpdate(0.8) → CREATE_ACTIVE」豁免（用户批准），与冲突 UPDATE 门槛同值、口径一致
- **M-5 同 key 多候选原子回滚边界**：单批同单值 key 两个不同显式值 → 第二个 save 撞唯一索引整批回滚（atomic 正确，连带丢弃 deletions）；Task 8 补 (key,value) 去重只覆盖完全重复项，同 key 异值保留现状（终审判定可推后）
- **MemoryExtractionInputAssembler 修订**（简报缺陷）：不能「仅删末尾 UserMessage」——真实 run 末条是 AssistantMessage，会漏当前轮进 context；正确=定位最后一条 UserMessage 整体截断（与 QU 同口径但适配 run 完成态）
- **@Component 缺注会静默挂全量集成测试**（Task 8 C-1）：新 @Service 构造器注入的无状态助手必须 @Component，否则 @SpringBootTest context 装载即挂、单测手工 new 掩盖——修后必须过一条 IntegrationTestBase 集成测试兜 wiring
- **SpotBugs REC_CATCH_EXCEPTION**（Task 3 遗留、Task 11 收口）：catch(Exception) 吃异常仅记日志被 spotbugs 拦 → 收窄 catch(JsonProcessingException)+catch(RuntimeException)，try 体穷举受检异常源=readTree 唯一，语义等价
- **worker 触发纪律**：doOnComplete 的 `errored` 短路保证仅 COMPLETED 触发；triggerPreferenceExtraction 三 null 兜底（userId/lastOutput/state）不破坏 doOnComplete 终态
- **deferred-minor 汇总**（终审分诊「必须修」为空，全部推后）：MemoryConfigTest 断言弱、value-synonyms yml 绑定无 Spring 测试、词表大小写敏感、getOrBuild null 防、size() 无消费方、集成用例 user_id 过滤、阈值内联硬编码等——详见账本

## 3. 进行中 / 未完成 (Pending)

- **计划 5/5（经历记忆）**：未开始。设计决策已定（记忆索引 s1-preference-episodic-memory-design + spec §8）。**复用本计划基建**：防抖队列机制（MemoryExtractionPipeline 骨架，5/5 按 §8.6 扩展 Episodic 分支）、注入预算结构（memory.preference 段 + 5/5 加 episodic 独立预算）、MemoryProperties 扩展、user_id 硬隔离模式。下一步：writing-plans 以 spec §8 生成计划 → 保存 `docs/superpowers/plans/2026-08-19-s1-plan5-episodic-memory.md` → 交用户审批（重大交付物需用户审批）
- **dev 手动验证**：偏好链路端到端手动验证标注为待办（run 后异步提取 → 决策落库 → 下轮 <preference> 注入 → 30min 冻结），可与计划 5/5 验证时一并做；附件链路已并入本计划验证时未做，一并列入
- **既定待办（非本计划引入）**：BUG-8 多实例部署（待批准）、前端两目录（等启动信号）、DOCX 图片路由验证（计划 1 遗留）、BM25 检索恢复（TASK.md §4 搁置）、计划 5/5 建议评估「同 key 异值取 explicitness 最高者」（终审 M-1）

## 4. 环境与依赖状态 (Environment)

- 与上一会话一致：JDK 17 + Maven 3.9.16（仓库 D:/code/envs/maven/3.9.16/repo）+ Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + Caffeine + PostgreSQL 16（Flyway V6-V11）+ Testcontainers（Docker 29.6.2 可用）
- **dev 启动**：`export PG_PORT=5433 REDIS_PORT=6380 && mvn.cmd spring-boot:run`
- **构建验证**：`cd backend && mvn.cmd clean verify`（925 测试；Windows 下 spotless:apply 转 CRLF 接受）
- **推送**：`git push --no-verify origin main`（GitHub HTTPS 间歇 reset，需多次重试）

## 5. 全局约定 (Locked Conventions，延续)

- **SDD 流程固定**：writing-plans → 用户审批 → SDD 子代理逐任务派发（实现子代理+任务审查+最多 5 修复轮+终审）；账本在 `.superpowers/sdd/<plan-basename>/`（git-ignored，保留供跨会话参考）
- **Git 纪律**：只 add 任务文件（禁 git add -A）；docs/ 审查/计划/进度文档不提交；每任务一提交；上 main 直接执行（前 4 计划同款，用户已批准）；push 走 HTTPS `--no-verify`（间歇 reset 重试收敛）
- **工程宪法**（AGENTS.md 全文）：中文注释/日志；禁全路径类名；@RequiredArgsConstructor + private final；禁循环依赖；本 service 主表 this.lambdaQuery()/lambdaUpdate()；先写 DB 后失效缓存；死代码零容忍；测试与实现同提交
- **记忆体系定案（本计划）**：偏好 user_preference 表（active/observing + 软删 0/1）+ write_score=0.4e+0.4s+0.2c + stability=min(1,0.1+count×0.15) + 显式表达(≥0.8)直达 active；推断注入预算 guaranteed 500 + 扩展 1500；30min 冻结缓存；提取=30s 防抖 + qwen3.7-flash + 10s 超时 + 失败丢弃；注入=HumanMessage 前置 + user_id 硬隔离
- **本周新增教训**（写进 memory）：@Component 缺注会挂全 @SpringBootTest；spotless 会把粘进来的简报代码转 CRLF；简报 Java 代码常有全路径类名/`*/` 闭合/断言与实现矛盾需要实现者最小修正（审查兜底）；MP this.lambdaQuery() 不可 mock 纯函数化；单批同 key 异值候选撞单值唯一索引→原子回滚（accepted）；spotbugs REC_CATCH_EXCEPTION 对「catch(Exception) 吃异常仅记日志」会拦
