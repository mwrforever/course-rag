# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。
> 本会话细分交接另见 `docs/progress/2026-08-20-S1计划5执行完成与推送.md`（计划 5/5 执行细节）。

## 元信息 (Meta)
- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-20T20:09:00+08:00
- 版本控制提交号: 4fc0c5b（S1 计划 1-5 全部代码 + 全套文档在远端 main，本地=远程=4fc0c5b，工作区干净）
- 任务一句话描述: S1 计划 5/5（经历记忆）SDD 执行完成并推送 + 全套 docs 与 TASK.md 入库——**S1 五份计划全部收官**

## 1. 已完成 (Completed) — 简要标记
- [x] S1 计划 5/5（经历记忆）SDD 全 10 任务执行完成（15 commits，100abdb..1f080fc）：
      V12 user_episodic_memory 表 + Milvus memory_chunks collection + EpisodicDecisionEngine（memory_score 门槛 + CREATE/UPDATE/MERGE/INVALIDATE 状态机）
      + IEpisodicMemoryService（落库 + Milvus 索引 best-effort 同步 + recall_history 动态召回）+ 提取流水线 Episodic 分支（独立执行器互不阻塞）
      + EpisodicInterceptor 注入（<episodic> 尾部 + 独立 1200 token 预算 + 当前/历史标注）+ RetrieveNode 召回集成
- [x] 整分支终审 clean（1 Important 死代码 + 1 Minor 空首项不对称，在终审修复 wave 1f080fc 全部解决）
- [x] 最终 HEAD 全量门禁：`mvn clean verify` BUILD SUCCESS——1000 测试全绿 / spotless 0 需改 / checkstyle 0 违例 / spotbugs 0 bug / jacoco check 全过；bundle 行覆盖 95.80%，决策引擎等 4 核心单类 100%、ServiceImpl 99.5%
- [x] 推送 origin/main：代码 1f080fc 一次成功；docs/TASK.md 提交（5770cd0 + 4fc0c5b）重试收敛；最终 HEAD=4fc0c5b
- [x] docs 全套入库（对象 15 文件计划与进度文档，含 2026-08-19-s1-plan5-episodic-memory.md 计划、2026-08-20-S1计划5执行完成与推送.md 交接）
- [x] S1 里程碑：计划 1/5 ETL 多模态 → 2/5 检索链 → 3/5 附件 → 4/5 偏好 → 5/5 经历记忆 全部完成

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 dev 手动验证：记忆双链路端到端 + 附件链路 + Milvus 活体冒烟
- 目标: 在 dev 环境人工验证「偏好记忆 + 经历记忆」端到端可用（run 后异步提取 → 决策落库 → 下轮召回/注入 → 偏好 30min 冻结），并补齐附件链路与 Milvus memory_chunks 的活体验证
- 当前状态: 未开始。测试对 Milvus 客户端为 mock-only（IntegrationTestBase @MockitoBean 策略），memory_chunks 的真实写入/召回未被活体验证；附件链路（计划 3/5 建立）验证被推移到本计划但未做
- 涉及文件/产物:
  - `backend` [修改] — 运行目标，验证期预期不改代码（除非发现问题）
  - `docs/progress/2026-08-20-S1计划5执行完成与推送.md` [修改] — 该文档已把此项列为待办
- 已锁定决策: run COMPLETED 后偏好/经历双通道独立异步提取（30s 防抖、独立执行器互不阻塞）；注入通道分离（<preference> 前端冻结 / <episodic> 尾部仅命中）；全链路 user_id 硬隔离
- 阻塞点/风险: 需要真实 DashScope 模型 api-key 与 Milvus/PG/Redis 服务在 dev 起；若发现链路缺陷需按 IDD 修复并补测回归；Milvus memory_chunks 的 SearchReq/UpsertReq 参数是照 SDK + SearchKnowledgeTool 先例写的，未经活体 Milvus 验证
- **下一步具体动作**: ① `export PG_PORT=5433 REDIS_PORT=6380 && mvn.cmd spring-boot:run`（在 backend/ 下）起 dev；② 用带明确偏好表达（如"以后都用中文"）+ 目标/进度陈述（如"准备转行 Java，正在学 Spring"）的会话跑一轮对话；③ 原始 SQL 查 user_preference / user_episodic_memory 确认落库（直接 SQL，MP @TableLogic 过滤注意）；④ 发起第二轮问题，观察 RetrieveNode/Eepisodic 日志是否 recall 命中并注入 <episodic>；⑤ 附件：上传一张图/一份文档复跑验证 caption/局部检索
- 验证方式: ① dev 起在 5433/6380 无冲突；②③ 两表出现符合预期的 active 行（content/importance/validity 正确）；④ 第二轮日志出现 recall 命中 + <episodic> 注入，回答体现记忆；⑤ 附件 caption/局部文档检索进入 document 上下文

### 2.2 deferred-minor 清理（内容去重 / 覆盖断言 / 命名一致性）
- 目标: 清理计划 5/5 终审分诊为「合入后跟随」的 deferred-minor，提升健壮性与可测性
- 当前状态: 全部已记录在 `.superpowers/sdd/2026-08-19-s1-plan5-episodic-memory/progress.md`（git-ignored，保留）；优先级排序：批内重复 CREATE 去重 > memory 索引参数 schema 断言 > 其余命名/注释类
- 涉及文件/产物:
  - `backend/src/main/java/com/commerce/rag/service/impl/EpisodicMemoryServiceImpl.java` [修改] — 批内同 content 双候选去重（对齐偏好 M-5 dedupeCandidates 先例）
  - `backend/src/test/java/com/commerce/rag/config/MilvusCollectionInitializerTest.java` [修改] — 补 memory 索引参数（HNSW/COSINE + user_id/type/validity INVERTED）断言
  - `backend/src/main/java/com/commerce/rag/config/MilvusCollectionInitializer.java` [修改] — schemaMatches 集合名三元分派可重构为 schema 派生（可选）
  - `backend/src/main/resources/prompts/episodic-extraction.yml` [修改] — <rules> 系统段 {existing} 字面量对齐 <existing> 块指代（可选）
- 已锁定决策: 打分门槛统一前置、merge_target 未命中降级口径、structured_facts 用 TEXT（V10 先例）——上述均不得改动
- 阻塞点/风险: 无硬阻塞；批内去重涉及 applyExtraction 执行语义，须补测试保证不误删合法并列记忆（多值同 type 不同 content 不受影响）
- **下一步具体动作**: 在 `EpisodicMemoryServiceImpl.applyExtraction` 决策循环前对同批 candidate 按 (type+content) 去重（保首，顺序不变），并补单测（同批两条相同 content → 只落 1 行）；随后补 MilvusCollectionInitializerTest 的 memory 索引参数断言（ArgumentCaptor 捕获 buildMemoryIndexParams 产物）
- 验证方式: 新增单测 + `cd backend && mvn.cmd clean test -Dtest='EpisodicMemoryServiceImplTest,MilvusCollectionInitializerTest' -DfailIfNoTests=false` 全绿；verify 门禁过

### 2.3 BUG-8 多实例部署（待用户批准）
- 目标: 解决多实例部署下的问题（按既有 bug 清单描述），需用户先批准方案
- 当前状态: 未开始，处于"待批准"——清单在 `docs/bugs/2026-08-16-24小时变更审查-待修复bug清单.md`
- 涉及文件/产物: `docs/bugs/2026-08-16-24小时变更审查-待修复bug清单.md` [未提交→已提交入库]
- 已锁定决策: 无（方案未定，需用户拍板）
- 阻塞点/风险: 依赖用户批准；多实例下 Redis Stream 消费组/会话锁/内存缓存语义需设计
- **下一步具体动作**: 向用户呈现 BUG-8 的具体问题面与候选方案（含多实例缓存一致性、XACK/巡检语义），获得批准后再进入 writing-plans
- 验证方式: 用户批准后按 SDD 流程出计划 → 执行 → 门禁绿

### 2.4 前端两目录落地 + 用户体验沟通（等待启动信号）
- 目标: 前端 AI 对话/消息渲染/会话界面（两目录）落地；**落地前必须与用户沟通**（AGENTS.md 硬约束）
- 当前状态: 未开始；管理端界面可按生产标准直接落地，C 端对话界面需先与用户对需求
- 涉及文件/产物: 前端目录 [待创建]（等用户给出目录与启动信号）
- 已锁定决策: C 端对话/消息渲染/会话界面前置沟通；管理端按生产标准直接落地
- 阻塞点/风险: 依赖用户给出前端目录位置与启动信号
- **下一步具体动作**: 向用户确认前端项目目录与启动信号 → 沟通对话界面需求 → 按 frontend-design 技能落地
- 验证方式: 前端构建/运行顺利；对话界面接入现有 SSE 协议（gateway-runs-and-sse 技能）

### 2.5 遗留工程项（待用户定夺/时机到再动）
- 目标: 逐项关闭既定遗留
- 当前状态:
  - Milvus sparse/BM25 检索恢复：**用户 2026-08-18 拍板搁置**，恢复方案候选已写进 `TASK.md` §4（等 SDK issue #1402 修复 / 应用侧 BM25 sparse 向量方案）；当前 `retrieval.sparse-enabled=false` dense-only 运行正常
  - DOCX 图片路由验证：计划 1/5 遗留，随 dev 手动验证（2.1）时可一并做
  - 计划 4/5 遗留「同 key 异值取 explicitness 最高者」：内容收敛口径，待评估
- 涉及文件/产物: `TASK.md` [已提交，§4 记录 BM25 搁置]；`backend/src/main/resources/application.yml` [retrieval.sparse-enabled]
- 已锁定决策: BM25 恢复暂缓（用户拍板）；附件链路保持纯向量（用户 2026-08-18 拍板）
- 阻塞点/风险: SDK EmbeddedText bug（milvus-io/milvus-sdk-java#1402）未修
- **下一步具体动作**: 无即时动作；2.1 dev 验证时顺带验 DOCX 路由；恢复 BM25 前须用户再确认走哪条候选方案
- 验证方式: 对应项按各自的验证口径（BM25 恢复后单/混合检索回归）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: JDK 17 / Maven 3.9.16（仓库 D:/code/envs/maven/3.9.16/repo）/ Spring Boot 3.5.8 / Spring AI 1.1.2 / SAA 1.1.2.0 / MyBatis-Plus 3.5.12 / milvus-sdk-java 2.6.11 / Caffeine / PostgreSQL 16（Flyway V6-V12）/ Redis / Testcontainers（Docker 29.6.2 可用）
- 关键配置: dev 端口 PG=5433、Redis=6380（以环境变量覆盖默认，见下）；DashScope 模型 api-key 在 application.yml 对应处
- 安装/构建/启动命令:
  ```bash
  cd "D:/code/project/commerce-customer/commerce-customer/backend"
  export PG_PORT=5433 REDIS_PORT=6380 && mvn.cmd spring-boot:run     # dev 启动
  mvn.cmd clean verify        # 全量门禁（spotless+checkstyle+spotbugs+jacoco，1000 测试）
  mvn.cmd test -Dtest=XxxTest -DfailIfNoTests=false    # 单类
  ```
- 推送: `git push --no-verify origin main`（GitHub HTTPS 会间歇 reset——连接超时/重置时加大间隔重试，本次曾有 6 连败后第 2 次收敛）

## 4. 全局约定 (Locked Conventions)
- **SDD 流程固定**：writing-plans → 计划保存 `docs/superpowers/plans/` → 交用户审批（重大交付物）→ SDD 子代理逐任务派发 + 任务审查（最多 5 修复轮）+ 终审 + 终审修复 wave；账本在 `.superpowers/sdd/<plan-basename>/`（git-ignored，保留供跨会话参考，勿删）
- **Git 纪律**：只 add 任务文件（禁 git add -A）；每任务一提交；上 main 直接执行；push 走 HTTPS `--no-verify` 重试收敛
- **docs 提交口径（本次会话用户指令确认）**：进度/计划文档可按用户要求随代码一并提交入库（本次 5770cd0 已把 S1 全套 docs 入库）——不再默认不可提交
- **工程宪法**（AGENTS.md 全文）：中文注释/日志；禁全路径类名；@RequiredArgsConstructor + private final；禁循环依赖；本 service 主表 this.lambdaQuery()/lambdaUpdate()；先写 DB 后同步索引；死代码零容忍（含测试文件内未用私有助手，终审 Policy 实证）；测试与实现同提交；`mvn clean verify` 是合入硬门禁（spotbugs Max 门禁下 catch(Exception) 必拦 REC_CATCH_EXCEPTION，收窄 RuntimeException；含 Future.get() 受检异常场景例外）
- **记忆体系定案（4/5+5/5）**：偏好 user_preference（write_score 0.4e+0.4s+0.2c + stability 曲线 + 观察池）/ 经历 user_episodic_memory（memory_score 0.4e+0.3c+0.3×(i×typeWeight) + validity 状态机）；提取=run COMPLETED 后 30s 防抖双通道独立执行器；注入=<preference> 前端冻结 + <episodic> 尾部仅命中；全链路 user_id 硬隔离；PG 为事实源 + Milvus 仅索引（best-effort 同步）；structured_facts 用 TEXT（V10 先例）
- **前端硬约束**：C 端 AI 对话/消息渲染/会话界面落地前必须与用户沟通；管理端界面按生产标准直接落地
