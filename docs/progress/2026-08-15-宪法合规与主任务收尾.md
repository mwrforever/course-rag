# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。
> 本会话上下文：上份交接文档 `docs/progress/2026-08-15-bug修正与新指示轮.md`（§2.1 主任务四项待确认 + §3 宪法合规波浪）。

## 元信息 (Meta)
- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-15T21:40:00+08:00
- 版本控制提交号: 7952066
- 任务一句话描述: **jacoco 补测达标（54.4%→80.1%，4 批次 11 提交 7952066 为最新）——宪法合规波浪与主任务收尾全部完成，verify 全门禁绿**

## 1. 已完成 (Completed) — 简要标记

### 1.1 宪法合规波浪（10 任务 11 提交 bc80eb2..23f3309，SDD 逐任务审查 + 最终审查 APPROVED）
- [x] **P1-6 ① 依赖注入**：10 个 service → `@RequiredArgsConstructor`（33 处 @Autowired 归零；DeviceKickService 手写构造器合法例外）
- [x] **P1-6 ② Wrapper 链式**：79 处 `new` → `Wrappers` 静态工厂（grep 归零）
- [x] **P1-6 ③ 全路径类名**：13 处引用 → import（grep 归零）
- [x] **P1-6 ④ MapStruct**：8 个转换器（SysUser/Course/Schedule/Enrollment/Document/KnowledgeBase/DocumentChunk/UserFeedback，service 包）；pom annotationProcessorPaths 三件套（lombok→binding 0.2.0→mapstruct 1.6.3）
- [x] **P1-6 ⑤ VO 化**：controller/vo 4 个 record VO（DocumentVO 无 sourcePath、DocumentChunkVO 无 denseVector，测试断言锁定）；4 个 admin controller 零 entity import
- [x] **P1-6 ⑥ 拼 SQL → XML**：DeviceKickService（FOR UPDATE 语义保持）+ ChatMessageService（foreach 批量）+ EnrollmentService（IN 查询 + 多源转换）——JdbcTemplate 依赖全移除
- [x] **perf P2-2 课程缓存**：CourseQueryService 4 查询 Caffeine（TTL 5min/512）+ evictCourse 精确失效；钩子挂 CourseService 5 写方法 + CourseScheduleService 3 写方法
- [x] **perf P2-3 统计缓存**：DashboardService 3 统计 Caffeine（TTL 60s/32）；失效挂 DocumentService 4 方法 + EtlPipeline.updateDocStatus + UserFeedbackService；@Qualifier 显式化 5 处
- [x] **P0-2 git 基线**：23f3309（250 文件入库，backend 218 个；.gitignore 增 .superpowers/.zcode/.workbuddy/.claude + Maven 调试输出）

### 1.2 主任务确认波浪（用户 2026-08-15 晚会话拍板四项设计，4 提交 6fdaffa/953455d/35f6e59/047a069）
- [x] **dashboard 口径回归全局**（6fdaffa）：三端点撤销 created_by 过滤（TEACHER 与 SUPER_ADMIN 同视图）；删 UserFeedbackMapper.xml/DocumentChunkMapper.xml 的 createdBy 参数与教师子查询（selectPendingChunkCountByTeacher、反馈版 selectPageFilteredByTeacher）；保留新口径（documentCount+pendingChunkCount+kbCount / studentCount+feedbackCount+likeRate / 每日趋势）与单条 SQL；缓存键简化（全局单键/period/days）；3 个废弃教师隔离测试删除（304→301）
- [x] **ChatRequestWorkerTest 红测修复**（953455d）：对齐 P3-2 读即 ACK——ACK 在 consumeLoop 读消息即确认，processRequest 断言 `never acknowledge`
- [x] **checkstyle includes 限定 src**（35f6e59）：verify 时 target/generated-sources 的 MapStruct Impl 误扫 85 违规（根因：verify 编译后 generated-sources 加入编译源根；excludes 对绝对路径不生效）→ `<includes>src/main/java/**/*.java,src/test/java/**/*.java</includes>`
- [x] **courseId 上传/文档级同步验证**：DocumentService.upload courseId 参数（默认 DEFAULT）+ EtlPipeline.syncDocToMilvus/syncChunkToMilvus + V6 course_id 索引——完整落地

### 1.3 【本次会话】jacoco 补测达标（54.4% → 80.1%，4 批次 11 提交 af1ccc2..7952066）
- [x] **批次 A**（af1ccc2，54.4%→62.4%）：config 0% 类（MyBatisPlus/Worker/Etl/F3/Minio）+ controller 0% 类（Student/AdminCourse/AdminSession/AdminSchedule/AdminKnowledgeBase/AdminLoginRecord/AdminEnrollment/AdminFeedback/Feedback）——68 测试
- [x] **批次 B**（bff2e8c，62.4%→67.8%）：service 0% 五类（Enrollment/SysLoginRecord/ChatSession/ChatMessage/ChatRun）——43 测试
- [x] **批次 C**（608d6e8，67.8%→73.1%）：retrieval FusionService + bot（QueryRewriter/PromptLoader/GraphConfig/LeadAgentGraph/CoalescingInterceptor/ReminderHook）——25 测试
- [x] **批次 D**（acc036d + 7952066，73.1%→80.1%）：9 文件扩展（AdminChunk/AdminDocument controller 全端点、CourseSchedule/KnowledgeBase/SysUser/Document/DocumentChunk/Course service、SseEventTransformer 工具结果→SOURCES）
- [x] **verify 全门禁绿**（实测 2026-08-15 晚）：`mvn verify` BUILD SUCCESS——spotless ✓ checkstyle ✓ spotbugs 0 bugs ✓ **jacoco LINE COVEREDRATIO 80.1% ≥ 0.80 ✓**；全量测试 480 个全绿
- [x] **豁免记录**：MilvusConfig 单测删除（MilvusClientV2 构造器 withWaitForReady 真实等连 10s 超时，无法无外部依赖单测，6 行）；GraphConfig postgresSaver 单测豁免（PGSimpleDataSource 建连，外部依赖）；QueryRewriterTest 走真实 DefaultChatClient mock ChatModel.call(Prompt)（Generation 需 AssistantMessage 包装）

### 1.4 其他核实
- [x] 24h 审查清单 4 项（P0-6 实证/P2-12 空 name 建库/P3-4 SecurityConfig 残留/P1-6 补充）已逐一核实全部修复
- [x] 进度文档与记忆已更新（docs/progress/2026-08-15-bug修正与新指示轮.md §3；记忆 constitution-compliance-wave-complete）

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 ~~jacoco 行覆盖率 54% → 80% 补测~~ 【已完成 2026-08-15 晚，80.1%】
- ~~目标: `cd backend && mvn.cmd verify` 全绿（jacoco LINE COVEREDRATIO ≥ 0.80）~~
- ~~当前状态: 2026-08-15 晚实测 54%（145 类，51 类 <40%）；基线 37%（2026-08-12）→ 54% 持续改善中；补测路线图在根目录 TASK.md；所有其他门禁已通过~~
- 达成记录: 4 批次 11 提交；`mvn verify` BUILD SUCCESS；jacoco 80.1%（covered=3803/4750）；全量测试 480 全绿
- 遗留低覆盖类（非阻塞，后续可选）：DeviceKickService 44%、EtlPipeline 37%、CustomSummarizationHook 35%、CourseQueryService 20%（Db 静态工具需 mockStatic）、ChatController 69%（reconnect/replayFromPg 需 SAA 流 mock）

### 2.2 观察项（基线盲区，下次波浪立项，本次不修）
- 目标: 消除剩余宪法违规存量（3 项，均为最终审查发现的基线盲区）
- 当前状态: 已实锤定位
- 涉及文件/产物:
  - `backend/src/main/java/com/commerce/rag/controller/AdminLoginRecordController.java:53-54` [修改] — 直出 `PageResponse<SysLoginRecord>`（Entity 出边界真违规）
  - `backend/src/main/java/com/commerce/rag/controller/AdminSessionController.java` [修改] — toSummaryMap 手写转换
  - `backend/src/main/java/com/commerce/rag/controller/AdminEnrollmentController.java` [修改] — controller 内 toDTO 转换
- 已锁定决策: 修法沿用宪法（VO 化 controller/vo 包 + MapStruct 转换器 service 包）；原观察①-⑤（StudentController 出 Entity / AuthController 直触实体 / service 反向依赖 controller/dto 包 / FeedbackController.create 返回实体 / Db 静态工具）继续有效
- 阻塞点/风险: 低风险；注意改后必须 `mvn.cmd clean` 重编译（MapStruct）
- **下一步具体动作**: ①AdminLoginRecordController 出参改 VO（SysLoginRecordVO 放 controller/vo，不含敏感列）+ AdminLoginRecordConverter；②AdminSessionController/AdminEnrollmentController 手写转换改 MapStruct；③适配对应测试；④全量门禁
- 验证方式: `mvn.cmd verify` 全绿 + `grep -rn "entity\." controller/` 归零

### 2.3 Deferred minors（低优先，可随时处理）
- CacheConfigTest 未断言 TTL 具体值（5min/60s/512/32）；CourseConverter 接口常量（Logger/ObjectMapper）可改注入 Spring bean；parseTags null/blank 分支无直测；缓存共享可变实例（消费者只读，风险可控）；DeviceKickServiceTest setUp 注释措辞过强；EtlPipeline PARSING 阶段失效略过度覆盖（成本可忽略）
- 完整清单在 `.superpowers/sdd/2026-08-15-constitution-compliance-and-cache/progress.md`（ledger）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17）、Maven 3.9.16（D:\code\envs\maven\3.9.16，仓库 D:\code\envs\maven\3.9.16\repo）；Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + MapStruct 1.6.3 + Caffeine（Boot BOM）
- 关键配置: `backend/.env` 只含 DASHSCOPE_API_KEY（gitignore）；PG/Redis/MinIO 凭证与 docker-compose.dev.yml 一致；**mapstruct/caffeine jar 已联网下载入库**（无需再授权）
- 安装/构建/启动命令:
  ```bash
  docker compose -f docker-compose.dev.yml up -d   # 基础设施
  cd backend && mvn.cmd test                        # 全量测试（当前 480/480）
  cd backend && mvn.cmd verify                      # 全门禁（spotless+checkstyle+spotbugs+jacoco；**当前全绿 80.1%**）
  cd backend && mvn.cmd spotless:apply              # 格式化（提交前自证）
  ```

## 4. 全局约定 (Locked Conventions)
- AGENTS.md 为纯工程宪法：分层依赖（controller→service→mapper）/ Entity 不出 service 边界 / DTO·VO 各层独立 / **层间转换必须 MapStruct 禁手写**（转换器与使用 Service 同包命名 XxxConverter）/ Wrapper 一律 Wrappers 链式禁 new / 禁业务层拼 SQL（复杂 SQL 走 mapper XML）/ 缓存先写 DB 后失效 / 注释日志全中文 / 查询按需字段 + 分页
- **MapStruct 改造后必须 `mvn.cmd clean` 重编译**（增量编译不重新生成实现类）
- **checkstyle 只查 src 源码**（pom includes 已限定；target/generated-sources 的 MapStruct Impl 不检查）
- superpowers 流程固定：核验（源码实锤）→ spec 审批 → plan 审批 → SDD；用户决策必须源码/官方文档实锤；重大交付物需用户审批
- git 纪律：`git add` 只加任务文件禁 `git add -A`（基线提交已完成，不再有例外场景）；提交 `--no-verify`（pre-commit hook 历史遗留阻塞，新机器 clone 后 `pre-commit install` 验证）
- 开发库无业务数据可 drop 重建；V6/V7 迁移直接改 + drop 重建，不加增量迁移
- 前端（frontend/ 与 student-frontend/）为空目录未实现；前端对话界面落地前必须与用户沟通
