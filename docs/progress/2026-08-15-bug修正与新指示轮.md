# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)
- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-15T17:50:55+08:00
- 版本控制提交号: 7576ed0（本会话**零提交**，全部改动在工作区未提交）
- 任务一句话描述: bug 清单全量核验修复 + 用户新指示轮（courseId 上传/文档级同步/dashboard 教师维度/P3 定案）；**新会话必须先与用户重新确认「新指示轮」设计再继续**

## 1. 已完成 (Completed) — 简要标记

### 1.1 已确认闭环（用户已拍板或为既定修复，无需重新确认）
- [x] **bug 清单 + 性能报告全量核验**: 30 项 bug + 9 项性能全部源码实锤（无误报），逐条追溯到代码行
- [x] **修复波 1-5（22 项 bug + 6 项性能）**: P0 全 8 项（课程删 Milvus/登出吊销/排期越权/事务自调用/参数 400/onErrorResume 兜底/删异常上抛）、P1 全 6 项（回放保序/payload 对齐/内联 404/终态竞态/状态重试/Logger 与 TestHelper）、P2 九项、P3-4、性能 6 项（MinIO 批量/批量校验/下载一次查询/findStats 单 SQL/教师子查询/cache 清理）；全量测试 275/275 + spotless + checkstyle 通过（**在该轮结束时已验证**）
- [x] **P3-3 定案**: SAA 1.1.2.0 浅拷贝行为已反编译实证（AppendStrategy new ArrayList / updateState Stream.collect 不可变），用户确认无需修复，从观察项划掉
- [x] **P2-4 角色门禁**: ChatController 加 `@PreAuthorize("hasAnyRole('STUDENT','TEACHER','SUPER_ADMIN')")`（用户裁决：允许 B 端角色对话）
- [x] **P3-1 调研结论（源码实锤，SAA 1.1.2.0）**: 孤儿 THINKING_END **不存在**——`NodeExecutor.transformFluxToGraphResponse` 对 agent model 节点的 AGENT_MODEL_FINISHED 事件显式置 message=null（`shouldOmitMessageOnStreamCompletion`，源码注释 "FINISHED StreamingOutput omits message for agent/graph LLM nodes"），transformer 的 FINISHED 分支恒空转；**附带发现**：SSE 的 TOOL_CALL 事件因在死分支里从未真实发出过，已把 TOOL_CALL 提取移到 AGENT_MODEL_STREAMING 分支（真实链路生效），FINISHED 分支保留为防御性并注释实证依据

### 1.2 用户新指示轮已落地（**待用户重新确认**，用户 2026-08-15 曾言"还在定稿呢"）
- [ ] **上传带 courseId（用户指示：前端录入时带课程归属，不传默认）**: V6 迁移 document 表加 `course_id VARCHAR(64) DEFAULT 'DEFAULT'` + 索引；Document 实体加字段；upload 接口/Service 加可选 courseId 参数；EtlPipeline.chunkDocument 分片继承文档 course_id（替代写死 DEFAULT）
- [ ] **文档级同步（用户指示：后台提供文档级同步而非单 chunk）**: 新增 `EtlPipeline.syncDocToMilvus(docId)`（整篇文档分片重建 Milvus 行，向量从 PG dense_vector 恢复不重新 embedding）；D7 batchUpdate 改为按涉及 docId 去重后逐文档同步；D5 单分片标注保留 syncChunkToMilvus
- [ ] **dashboard 口径重构（用户 2026-08-15 定稿：会话数无业务意义；所有人都能看到，不区分是谁的学生）**: 三端点**回归全局统计（无 created_by 过滤，TEACHER 与 SUPER_ADMIN 所见一致）**；口径变化保留——dashboardStats = documentCount + pendingChunkCount + knowledgeBaseCount（新增 kb 数），feedbackStats 的 sessionCount 换成 studentCount + feedbackCount + likeRate，feedbackTrend 每日反馈数；**注意：当前已落地的代码是「教师隔离版」（按 created_by/学生归属过滤），新会话必须撤销过滤回归全局**（含 I1 列表/I2 统计的教师隔离、UserFeedbackMapper.xml/DocumentChunkMapper.xml 的 createdBy 参数与教师子查询方法），但保留新口径与单条 SQL 优化
- [ ] **P3-2 读到即 ACK（用户指示：从 run 拿到任务直接 ack 不等执行完成）**: consumeLoop 读到消息立即 ACK（submit 前），删除 reclaimPending/XCLAIM 机制、scheduler、staleLeaseMinutes 配置（application.yml 已删）；语义变为 at-most-once，run 失败由 chat_run 状态兜底

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 【第一优先】用户重新确认「新指示轮」设计（阻塞后续一切）
- 目标: 用户对 1.2 四项设计定稿（courseId 上传/文档级同步/dashboard 口径/P3-2 读即 ACK）；dashboard 已定稿（全局可见不区分学生归属，见 §1.2），其余三项待确认
- 当前状态: 代码已全部落地（工作区未提交）；**P3-1/P3-2 修改后的全量测试未跑完**（最后一条 `mvn test` 被用户中断，P3-1 新增的 STREAMING TOOL_CALL 测试与 P3-2 构造修改后的测试状态未验证）
- 涉及文件/产物: 见 1.2 各项（V6 迁移 / Document.java / DocumentService / AdminDocumentController / EtlPipeline / DocumentChunkService / DashboardService / AdminDashboardController / AdminFeedbackController / UserFeedbackService / UserFeedbackMapper.xml / DocumentChunkMapper.xml / ChatRequestWorker / application.yml / ChatController / SseEventTransformer）
- 已锁定决策: 用户明确拍板的：B 端角色允许对话（P2-4）；P3-3 无需修；MapStruct 禁止手写、依赖可以加载；P3-2 读即 ACK
- 阻塞点/风险: 用户中断前说"还在定稿呢"——1.2 四项（尤其 courseId 方案与 dashboard 口径）用户尚未最终确认，新会话**必须先展示设计/改动让用户确认**，不得直接继续
- **下一步具体动作**: 新会话打开项目后：①读本文件 §1.2 四项设计摘要，向用户逐项确认（courseId 是否按「文档级存 document.course_id、分片继承」、dashboard 口径是否接受 studentCount/feedbackCount、P3-2 at-most-once 是否接受）；②确认后先跑 `cd backend && mvn.cmd test` 验证 P3-1/P3-2 修改后的全量测试（预期全绿，含新增 STREAMING TOOL_CALL 用例）
- 验证方式: 用户口头确认 + `mvn.cmd test` 全绿

### 2.2 P1-6 宪法合规组（用户已明确"必须做"，尚未执行）
- 目标: 消除 AGENTS.md 宪法违规存量：①10 个 service @Autowired 字段注入 → private final + @RequiredArgsConstructor；②40+ 处 new LambdaQueryWrapper/LambdaUpdateWrapper → Wrappers 链式；③全路径类名 → import（AuthController/CourseService/AdminDocumentController/DeviceKickService/PromptLoader/CustomSummarizationHook/MilvusCollectionInitializer）；④手写转换 → **MapStruct**（用户禁止手写，转换器与使用它的 Service 同包命名 XxxConverter）；⑤Entity 出边界 → VO 化（AdminDocumentController/AdminKnowledgeBaseController/AdminChunkController/AdminFeedbackController，VO 放 controller/vo 包，DocumentChunkVO 不得含 denseVector、DocumentVO 不得含 sourcePath）；⑥DeviceKickService JdbcTemplate 拼 SQL → mapper XML
- 当前状态: 未开始
- 涉及文件/产物: 10 个 service + 4 个 controller + DeviceKickService + pom.xml（mapstruct + caffeine 依赖）+ 新建 VO/Converter + CourseQueryService/DashboardService 缓存
- 已锁定决策: 用户明确「MapStruct jar 可以加载依赖，千万别手写」；**perf P2-2/P2-3 Caffeine 缓存 = 联网必做**（用户 2026-08-15 定稿），与 MapStruct 同批处理
- 阻塞点/风险: 本地 Maven 仓库（D:/code/envs/maven/3.9.16/repo）无 mapstruct/caffeine jar——pom.xml 加依赖后 `mvn.cmd dependency:resolve` 联网下载（用户已授权）；**依赖引入后须 clean 再编译**（宪法要求：转换接口或 DTO 修改后增量编译不重新生成实现类）
- **下一步具体动作**: ①pom.xml 加 `org.mapstruct:mapstruct` + `org.mapstruct:mapstruct-processor`（1.6.x，Java 17 兼容）+ `com.github.ben-manes.caffeine:caffeine`；②`mvn.cmd clean compile` 验证依赖可下载；③perf P2-2：CourseQueryService 查询加 Caffeine（TTL 5-10min，失效钩子挂 CourseService.createCourse/updateCourse/deleteCourse/updateContent/batchUpdateContents/排期变更按 courseId 精确失效，一致性铁律先写 DB 后失效缓存）；④perf P2-3：DashboardService 统计加 Caffeine（TTL 60s 或按数据变更失效：上传/ETL 终态/反馈提交）；⑤按 a→f 顺序逐文件合规（每文件改完跑对应单测）
- 验证方式: `mvn.cmd test` 全绿 + `grep -rn "new LambdaQueryWrapper\|@Autowired" backend/src/main/java` 归零 + spotless/checkstyle 通过

### 2.3 P0-2 git 基线提交（用户已明确"必须做"，尚未执行）
- 目标: backend 138 个 Java 文件仅 34 个被 git 跟踪（fresh checkout 不可编译），一次基线提交全部入库
- 当前状态: 工作区有本会话全部改动 + 历史遗留（旧根结构 src/ 删除、pom.xml 删除、docker-compose.yml/.gitignore 修改、backend/ 大量未跟踪文件）
- 已锁定决策: 用户明确「② 既定计划项必须做」= 基线提交落地；git 纪律例外（平时禁 add -A，基线提交是用户授权的例外）
- 阻塞点/风险: 提交内容巨大（全部 backend 文件 + 旧结构删除）；建议在 P1-6 合规组完成后一次性提交（避免两次大提交）；提交信息建议「chore: backend 全量基线提交（126+ 文件首次入库）+ 2026-08-15 修复波」
- **下一步具体动作**: 待 2.1 用户确认 + 2.2 合规组完成、全量测试门禁全绿后：`git add -A && git commit`（用户授权范围内），或先问用户是否此时提交
- 验证方式: `git ls-files backend/ | wc -l` 接近全量文件数；fresh checkout 可编译

### 2.4 遗留观察/报告项（维持不动，仅记录）
- P2-4 已定案（见 1.1）；P2-8/P2-9 已按用户指示重做为教师维度（见 1.2，待确认）；P3-1 已调研定案（见 1.1）；P3-2 已简化（见 1.2，待确认）；P3-3 已定案
- perf P1-1 ETL 批量化：与 S1 多模态 RAG 的 ETL 重写重叠，留待 S1
- perf P2-2/P2-3 Caffeine 缓存：**用户定稿 = 联网必做**（与 MapStruct 同批处理，见 §2.2）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17）、Maven 3.9.16（D:\code\envs\maven\3.9.16，仓库 D:\code\envs\maven\3.9.16\repo）；Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + PG 驱动 42.7.8
- 关键配置: `backend/.env` 只含 DASHSCOPE_API_KEY（gitignore）；PG/Redis/MinIO 凭证与 docker-compose.dev.yml 一致；**本地仓库无 mapstruct 与 caffeine jar，加载依赖需联网**（用户已授权 mapstruct）
- 安装/构建/启动命令:
  ```bash
  docker compose -f docker-compose.dev.yml up -d   # 基础设施
  cd backend && mvn.cmd test                        # 全量测试（本会话最终验证点：P3-1/P3-2 修改后未跑完）
  cd backend && mvn.cmd spotless:apply && mvn.cmd checkstyle:check   # 门禁
  ```

## 4. 全局约定 (Locked Conventions)
- AGENTS.md 为纯工程宪法：分层依赖/对象通信（Entity 不出 service 边界、DTO/VO 各层独立禁跨层复用、层间转换必须 MapStruct 禁手写）/目录职责/并发异步/MyBatis-Plus 规范（Wrappers 链式禁 new、复杂 SQL 走 mapper XML）/缓存一致性/代码规范（注释日志全中文、UTF-8 无 BOM）
- superpowers 流程固定：核验（源码实锤）→ spec 审批 → plan 审批 → SDD；**用户决策必须源码/官方文档实锤**
- git 纪律：工作区有大量未提交改动（历史遗留 + 本会话），`git add` 只加任务文件禁 `git add -A`（**基线提交是用户授权的唯一例外**）；开发库无业务数据可 drop 重建；V6/V7 迁移直接改 + drop 重建，不加增量迁移
- 前端（frontend/ 与 student-frontend/）为空目录未实现；前端对话界面落地前必须与用户沟通
- 本会话关键实证：SAA 1.1.2.0 的 FINISHED 事件对 agent model 节点 message=null（P3-1 依据）；本地仓库缺 mapstruct/caffeine jar
