# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。

## 元信息 (Meta)

- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-18T10:30:00+08:00
- 版本控制提交号: 87f75f1（已 push 至 origin/main，远程已核实一致）
- 任务一句话描述: **S1 多模态 RAG 主任务——计划 1/5「ETL 多模态数据底座」SDD 全量执行完成并推送；下一步计划 2/5 检索链路重构 + 开发环境遗留手动验证**

## 1. 已完成 (Completed) — 简要标记

- [x] **S1 计划 1/5（ETL 多模态数据底座）14 commits 完成并推送**：文档 `docs/superpowers/plans/2026-08-17-s1-plan1-etl-multimodal-foundation.md`（用户已批准）；SDD 子代理逐任务派发（2963d30..87f75f1）；`mvn clean verify` 783 测试全绿、jacoco line 95.50%、spotless/checkstyle/spotbugs 零违规、新增类单类 ≥0.84
- [x] **交付组件**：document_chunk 三列（content_type/image_url/sha256）+ sha256 索引（V6 直改，dev 库需 drop 重建生效）；Milvus knowledge_chunks 重建（describe 比对自动 drop，去 collection_type 加三字段）；XHTML 结构化解析（标题路径栈/表格分区/内嵌图片捕获）；TokenTextSplitter 文本分片 + 表格 Markdown/行分组 + 图片 VLM caption（base64 data URL）；SHA256 归一化全局去重（含 `.ne(docId)` 终审加固）；模型配置 qwen3.8-max / qwen3.7-text-embedding / caption qwen3.7-flash
- [x] **推送**：origin/main = 87f75f1（HTTPS 第 7 次重试命中稳定窗口；`--no-verify` 按既定约定跳过 pre-push Maven）

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 S1 计划 2/5「检索链路重构」（计划已生成，待审批后 SDD）

- 目标: 按 S1 spec（docs/superpowers/specs/2026-08-12-multimodal-rag-design.md，2026-08-14 终版）§1-3 实施检索链路：Query Understanding（intent+重写+filters+recall_history）、CourseNameMapper、RetrieveNode、ContextBuilderService、DocumentAssemblerInterceptor、prompt 三件套（system-base/agent-instruction/query-understanding）、LeadAgentGraph 三节点分支、SearchKnowledgeTool 去 @Tool 化、IntentType 值域改造
- 当前状态: **计划文档已生成（2026-08-18）**：`docs/superpowers/plans/2026-08-18-s1-plan2-retrieval-chain-refactor.md`（11 个任务，含完整失败测试/实现/prompt 定稿/删除清单，已做 spec 覆盖自审与测试 stub 正确性修正）；**待用户审批 → SDD 逐任务派发**（工作区 `.superpowers/sdd/2026-08-18-s1-plan2-retrieval-chain-refactor/`，新计划新账本）
- 涉及文件/产物:
  - 新建 6 个主类：`bot/rewrite/QueryPlan(+Filters)`、`bot/rewrite/QueryUnderstandingService`、`retrieval/CourseNameMapper`、`retrieval/ContextBuilderService`、`bot/graph/RetrieveNode`、`bot/hook/DocumentAssemblerInterceptor`
  - 修改：`LeadAgentGraph`（三节点+条件边）、`SearchKnowledgeTool`（去 @Tool+sha256 去重）、`KnowledgeSearchResult.KnowledgeChunk`（+sha256）、`RerankService`、`IntentType`、`OverAllState`（+queryPlan）、`GraphConfig`（+queryPlan 策略）、`ICourseQueryService(+findByTitle)`、`ReminderHook`（简化）、prompt 四件（system-base/agent-instruction 定稿替换 + dynamic-context 简化 + query-understanding 新建）、application.yml（rag.query-understanding.* / rag.context-builder.top-k / retrieval.prefetch-top-k）
  - 删除：`QueryRewriter` + 其测试 + `query-rewrite.yml`（死代码零容忍，Task 10 同提交）
- 已锁定决策（沿用 spec + 项目记忆，新会话不得推翻）:
  - 三节点 `START → queryUnderstandingNode →(条件边 knowledge_question→retrieveNode / chat|unknown→reactAgent)→ reactAgent → END`；SAA addConditionalEdges(AsyncEdgeActionWithConfig) 与 ModelRequest.getContext() 已 javap 实锤
  - document 注入走 ModelInterceptor（DocumentAssemblerInterceptor，幂等，不落 checkpoint）；传递通道 config.metadata() → request.getContext()（SAA 源码实锤，见 s1-document-assembly-saa 记忆）
  - SHA256 去重在 fusion 后 rerank 前（保留 RRF 分数最高一条，8 字段 KnowledgeChunk 带 sha256）；Top-N=rag.context-builder.top-k 默认 5；预取 retrieval.prefetch-top-k 默认 20
  - QU 独立模型 qwen3.7-flash（rag.query-understanding.model）；QueryPlan 走 JsonNode 手工解析 + IntentType.fromString 宽松映射；降级 unknown+原查询单条不拒答
  - filters 首版只 course_names：CourseNameMapper 查库映射（同名多课全注入、失败降级全局）；course_id 是相关性收窄非权限
  - user-document 子块首版不组装（属计划 3/5 附件链路）；ReminderHook 去重写查询提醒（连带修改）
  - 任务 8 提前定义 OverAllState.KEY_QUERY_PLAN，任务 9 补 GraphConfig 策略（同计划内 TDD 依赖）
- 阻塞点/风险: 无；计划审批后 SDD 执行；期间中间提交勿在 dev 验证聊天检索链路（Task 3 起 prompt 去 searchKnowledge 指引、Task 10 才恢复）
- **下一步具体动作**: 计划审批 → SDD 逐任务派发（每任务 = 实现子代理 + 任务审查 + 最多 5 修复轮 + 终审）→ 每任务 `cd backend && mvn.cmd test -Dtest=...` 红→绿 → 全量 `mvn.cmd clean verify` 收尾
- 验证方式: 计划文档获批；SDD 首任务完成且 verify 全绿；Task 11 遗留手动验证清单（dev 提问观察 queryUnderstandingNode/retrieveNode 日志、intent=chat/unknown 分支、courseNames 过滤与全局降级）

### 2.2 开发环境遗留手动验证（合并后执行，非自动门禁）

- 目标: 验证本计划落地后 dev 环境全链路可用（CI 单测无法覆盖的部分）
- 当前状态: 未执行（需 dev MinIO/DashScope/Milvus 真实环境）
- 涉及文件/产物: 无代码改动，仅验证；风险确认记录入本章
- 已锁定决策: dev 库无业务数据，可 drop 重建（用户已拍板）
- 阻塞点/风险: GitHub 网络不稳已解决（推送完成）；Docker 需手动启动（docker compose -f docker-compose.dev.yml up -d）；**DOCX 图片经 Tika 2.9.2 路由到 EmbeddedDocumentExtractor 待真实文档验证（计划决策点 4 风险）**；无 courseIds 全局检索的 Milvus 过滤器 null 路径待验证
- **下一步具体动作**（按序）:
  1. `docker compose -f docker-compose.dev.yml up -d` 起 PSG/Redis/MinIO/Milvus；执行 `DROP DATABASE IF EXISTS commerce_rag; CREATE DATABASE commerce_rag;`（psql）后重启后端 → Flyway 迁移 V6 三列生效
  2. 观察后端启动日志：Milvus「schema 不匹配，drop 重建」→「创建成功」→「加载完成」
  3. 管理端上传一份含表格+内嵌图片的 PDF → ETL 日志核对：XHTML 字符数/捕获图片数 → 分片（text/table/image）→ SHA256 去重 → INDEXED
  4. 学生端发知识问答（含一次不带课程上下文的纯知识问）→ 检索可用（覆盖全局检索路径）；如上传 DOCX 验证图片路由
  5. 观察 caption 真实模型调用成功（qwen3.7-flash）
- 验证方式: 上述各步日志与检索结果符合预期

### 2.3 后续计划（3/5 附件 / 4/5 偏好 / 5/5 经历）与其它待办

- 目标: 完成 S1 全部五个子计划 + 既定待办
- 当前状态: 未开始（依赖 2/5 先行；3/5 消费 1/5 的解析/切分/caption 组件）
- 已锁定决策: 计划拆分见计划 1/5 文档「计划拆分总览」；偏好/经历记忆设计决策见项目记忆索引（s1-preference-episodic-memory-design 等）
- **下一步具体动作**: 2/5 完成后逐个按 writing-plans→审批→SDD 推进；各计划工作区独立（`.superpowers/sdd/<plan-basename>/`）
- 验证方式: 各计划文档获批 + 每计划 verify 全绿
- 另有既定待办: **BUG-8 多实例部署**（TASK.md §1，未经用户批准不得实现）；前端两目录仍空（等用户启动信号，AI 对话/渲染界面落地前必须与用户沟通）

## 3. 环境与依赖状态 (Environment)

- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17，PATH 可用）、Maven 3.9.16（D:\code\envs\maven\3.9.16，本地仓库 D:/code/envs/maven/3.9.16/repo）、Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + jsoup 1.18.1（本计划新增）+ Testcontainers（postgresql/redis）；Docker 29.6.2 运行中
- 关键配置: `backend/src/test/resources/application-test.yml`；`auth.strict-secret` 默认 false；新模型名 qwen3.8-max/qwen3.7-text-embedding/qwen3.7-flash（caption）
- 安装/构建/启动命令:
  ```bash
  cd D:\code\project\commerce-customer\commerce-customer\backend
  mvn.cmd clean verify        # 全门禁（当前 783 测试全绿）
  mvn.cmd test -Dtest=XxxTest # 单类
  mvn.cmd spotless:apply      # 格式化（Windows 转 CRLF，check 接受）
  docker compose -f docker-compose.dev.yml up -d
  ```

## 4. 全局约定 (Locked Conventions)

- **SDD 流程固定**：writing-plans 生成计划 → 用户审批 → SDD 子代理逐任务派发（每任务 = 实现子代理 + 任务审查 + 最多 5 修复轮 + 终审）；账本在仓库根 `.superpowers/sdd/<plan-basename>/progress.md`（git-ignored，含 deferred minors 与裁决、遗留手动验证清单）；子代理句柄失效时用新子代理 + 简报/报告文件续接
- **SDD 工作区解析**：脚本须在仓库根目录执行（`cd D:\code\project\commerce-customer\commerce-customer` 再跑 scripts/review-package / sdd-workspace / task-brief），否则解析到错误路径
- **计划 1 关键决策（不得推翻）**：TokenTextSplitter 1.1.2 无 overlap 参数（字节码实锤）→ 采用句子边界回卷 + 过小合并，`etl.chunk.overlap` 已删；PG document_chunk.collection_type 列保留（admin 校正工作流用），Milvus 侧已移除；去重查询含 `.ne(docId)` 排除本文档自身
- **Git 纪律**：只 add 任务文件、禁 git add -A；docs/ 下审查报告与计划/进度文档不纳入提交（历史计划文档除外——本会话计划文档未提交）；push 走 HTTPS（origin push URL 已切 https，fetch 仍 ssh），hook 全过后 `git push --no-verify` 加速重试
- **工程宪法**（AGENTS.md 全文）：注释/日志中文；禁全路径类名；@RequiredArgsConstructor + private final；禁循环依赖；禁弃用 API；本 service 主表 this.lambdaQuery() 链式、wrapper 限非主表；先写 DB 后失效缓存；死代码零容忍；测试与实现同提交；新测试必须真实覆盖目标逻辑（本计划 4 个任务在审查中抓到假测试/空断言并修复，新计划须以此为戒）
- **模型搭配（spec §6）**：主对话 qwen3.8-max、VLM caption qwen3.7-flash、embedding qwen3.7-text-embedding（1024 维）、rerank qwen3-rerank；SAA 1.1.2 视觉输入 = Media.builder().data(byte[]) → base64 data URL（本地 MinIO 对 DashScope 云不可达，禁传 URL）
