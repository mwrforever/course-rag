# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。
> 本会话上下文：上份交接文档 `docs/progress/2026-08-16-合规修正全量实施完成.md`（458967c）。
> **本会话 = 24h 审查 bug/性能清单修复轮（0ffe128）+ 契约定稿与 L-11 核对（abbb73d）+ BUG-7 实现与 BUG-8 待办（da4a14e），全部 `mvn verify` 全门禁绿（736 测试）。**
> 变更要点：上份文档 §2.1 的 S1 主任务仍为唯一立项待办（本次会话按用户指示继续延后）；新增契约定稿文档（前端实现基准）、BUG-8 待批准待办、M-4 异步化决策结论。

## 元信息 (Meta)

- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-16T21:20:00+08:00
- 版本控制提交号: da4a14e（其后工作区仅剩两份审查报告未跟踪：docs/bugs/2026-08-16-24小时变更审查-待修复bug清单.md、docs/pref/2026-08-16-性能优化建议.md——为既有输入物，不属于任何提交）
- 任务一句话描述: **24h 审查 bug/性能清单全量修复（BUG-1/2 + H-1/2/3 + M-1~8 + L 项 + BUG-9/10/11，0ffe128）+ 契约类定稿与 L-11 核对（abbb73d）+ BUG-7 实现与 BUG-8 待办（da4a14e），verify 全绿 736 测试；剩余待办 = S1 主任务 + BUG-8（待批准）+ M-4 异步化（待管理端界面设计）**

## 1. 已完成 (Completed) — 简要标记

### 1.1 24h 审查修复轮（0ffe128，46 文件，+14 测试 → 731 全绿）

- [x] **BUG-1（中危，认证核心链路）**：markRefreshTokenUsedAtomic 降级 fail-open 不再写 PG 黑名单——原实现降级分支先写 PG 再返回 true，AuthController 随后 isBlacklisted 降级查 PG 命中自身刚写的行 → Redis 故障期间 refresh 必 401 且 RT 永久烧毁；补 DeviceKickService 层（never insert 断言）+ AuthController 层（Redis 故障 refresh 成功）回归测试
- [x] **BUG-2/M-2（中危，统计一致性）**：dashboard 统计缓存补失效 6 条写路径（DocumentChunk 删除/批量修正、KnowledgeBase 增删、SysUser 增删、Course 级联软删），均为「先写 DB 后失效」
- [x] **H-2（高，安全边界）**：设备互踢预读 TTL 竞态消除——删 Java 预读（TOCTOU），黑名单 TTL 固定全量传 Lua（kick_and_login.lua 注释同步修正），旧设备检测原子化于 Lua 内 GET
- [x] **H-1 + M-3（高/中，SSE 链路）**：MemoryStreamBridge 重构——每 run 有界投递队列（容量=ring 256）+ 独立投递线程（bridge-delivery-{runId}），SseEmitter.send 不再阻塞生成线程；replayAndSubscribe 锁内仅收集快照+入队回放批次（锁内不再发送），单投递线程 FIFO 保证「回放事件先于实时事件」；队列满 → 摘除全部订阅者（complete → EventSource 自动重连 + ring 回放补偿，事件不丢）；sendEvent 补 RuntimeException 捕获（与 complete 并发）；测试重写适配异步（awaitSendCount/awaitOutboxDrained 轮询助手 + 4 个新覆盖测试）
- [x] **H-3/M-1/M-4（中，ETL 批量化）**：embedding 批量（embed(List<String>)，DashScope 实证一次请求携带全部文本，批次 16 可配 etl.embedding-batch-size）；PG 向量回写单条 CASE WHEN 批量 UPDATE（batchUpdateVectors）；Milvus 多行 InsertReq（insertToMilvusBatch + buildMilvusRow）；next_chunk_id 链路单条批量回填（batchUpdateNextChunkIds，2N→N+1 SQL，**未全量 saveBatch——单测 mock mapper 无 SqlSessionFactory，Db.saveBatch 不可测**）；syncDocToMilvus 批量 delete（filter IN）+ 多行 insert
- [x] **M-5/M-6（中）**：findByCourseIdAsVO 按 ChunkVO 7 列投影（原全列含 dense_vector BYTEA）；cleanupExpiredBlacklist 单条 UPDATE（原先查回+循环更新，与 cleanupExpired 同范式）
- [x] **M-7/M-8（中，管道停摆）**：etlPool/runPool 改 AbortPolicy——上传/reparse 队列满回写文档 FAILED+503（submitEtlOrFail）；worker 消费队列满回写 run=ERROR（不再 CallerRuns 内联阻塞消费循环）；新增 ACTIVE run 巡检（worker.run-pool.stale-run-timeout-minutes: 10，每 60s 扫描置 ERROR 解锁 uniq_active_run_per_session）
- [x] **L-2/3/4/5/7/8/9/12/13 + BUG-9/10/11（低）**：消息查询按 ChatMessageVO 8 列投影；addStudents 分离「待激活/新建」集合批量 UPDATE+saveBatch（@Transactional）；findContext 相邻分片一次批量查询+投影（2 次查询替代 4 次全列 selectById）；checkOwnershipBatch kbId 去重批量查；心跳线程 2→4；删除只写不读的 chat:result 缓存（cacheFinalResult 全删）；CourseQuery/Dashboard 缓存改 Caffeine get(key, fn) 原子重建；WorkerConfig 线程数改 yml 生效（原 CPU*2 硬编码使 core/max 死配置）；PG 黑名单 TTL 按 token 类型（ACCESS→accessTokenExpiry）；新增 lombok.config（copyableAnnotations += @Qualifier，6 处字段注解随构造器参数携带）；CacheConfig 注释修正；WarningHook.cleanupSession 由 worker finally 调用（取消/异常路径不泄漏 DetectionState）
- [x] **doOnComplete 顺序调整**：先 persistMessages 后 handleCompleted——「run 终态=数据已完整落库」（消除 ChatFlowIntegrationTest 的 COMPLETED 轮询与消息查询竞态，该用例曾 flaky 失败）

### 1.2 契约定稿与核对轮（abbb73d，2 文件）

- [x] **契约类 BUG-3/5/6 定稿（前端未设计，后端现状即终态契约）**：ConcurrentRunException extends BizException(ErrorCode.CONFLICT=409) 全局统一 ApiResponse{code,message,data}（code=HTTP 状态码）；集合字段恒 []；可空对象字段恒 null 不缺 key；token_count 0=未统计——全部落盘 `docs/contracts/2026-08-16-接口契约定稿.md`（前端实现基准，前端开发时以该文件为准）
- [x] **L-11 DDL 核对**：correction_status 有 idx_document_chunk_correction、role 有 idx_sys_user_role_status（均已有）；**user_feedback.created_at 缺失 → 新增 V8__feedback_created_at_index.sql**（idx_user_feedback_created_at，deleted=0 部分索引，flyway 集成测试真实执行通过）
- [x] **M-4 异步化核对结论：不做**——批量化已解决主要痛点（每文档 2 次 Milvus 调用）；单分片秒级阻塞可接受；异步化需新增分片级向量状态字段+轮询契约（真契约设计），等管理端界面设计时一并定

### 1.3 BUG-7 实现与 BUG-8 待办（da4a14e，9 文件，+5 测试 → 736 全绿）

- [x] **BUG-7 JWT 密钥严格模式**：`auth.strict-secret`（默认 false 不拦截开发，生产设 true 时启动校验 JWT_SECRET 已配置且非内置默认值否则拒绝启动）；config/AuthSecretValidator（InitializingBean）+ 5 用例四分支
- [x] **BUG-8 按用户指示仅吸纳待办**：TASK.md 第 1 节「多实例部署—待用户批准立项」（worker-id 硬编码 1 跨实例主键冲突 + Caffeine 本地缓存跨实例脏读），**批准前不得实现**

### 1.4 最终验证实测数据（2026-08-16 21:00 跑 `mvn.cmd verify`）

| 门禁 | 结果 |
|---|---|
| 测试 | surefire 736 用例（0ffe128 时 731），Failures 0 / Errors 0 / Skipped 0 |
| spotless | clean（注意：Windows 上 spotless:apply 会把文件转 CRLF，check 接受；改文件后先 spotless:apply 再 verify） |
| checkstyle | 0 violations |
| spotbugs | BugInstance 0 |
| jacoco | BUNDLE + CLASS 全部达标（修复轮曾拦 3 类：MemoryStreamBridge.Ring/EtlPipeline/ChatRunServiceImpl → 补测后通过） |

## 2. 进行中 / 未完成 (Pending)

### 2.1 D1 S1 多模态 RAG 主任务（唯一立项待办，与上份交接文档一致，本次会话按用户指示继续延后）

- 目标: 按 S1 spec（docs/superpowers/specs/2026-08-12-multimodal-rag-design.md，2026-08-14 终版）实施多模态 RAG
- 当前状态: P0-P3 bug 修复 + 宪法合规 + 覆盖率达标 + 24h 审查修复全部完成；**既定下一步 = superpowers:writing-plans 以 S1 spec 生成实施计划 → SDD**
- 涉及文件/产物: `docs/superpowers/specs/2026-08-12-multimodal-rag-design.md` [修改-已终版]
- 已锁定决策: 见记忆索引（s1-metadata-filter-design/s1-query-understanding-design/s1-prompt-design/s1-document-assembly-saa/s1-etl-multimodal-decisions/s1-user-attachment-design/s1-preference-episodic-memory-design/s1-model-selection）
- 阻塞点/风险: 无（与已完成的修复轮无依赖冲突）；实施前需用户确认计划
- **下一步具体动作**: 用户批准启动后，用 superpowers:writing-plans 以 S1 spec 生成实施计划 → 用户审批 → SDD 分任务执行（首个任务完成后 `cd backend && mvn.cmd verify` 全绿）
- 验证方式: 计划文档产出且用户批准；首个 SDD 任务完成并 verify 全绿

### 2.2 BUG-8 多实例部署（待用户批准立项，已记 TASK.md 第 1 节）

- 目标: 多实例部署时雪花 ID 不冲突（worker-id/datacenter-id 分配或配置化）+ 本地缓存一致性方案
- 当前状态: 仅记录待办，**未实现**；用户 2026-08-16 明确指示「后续扩展，吸纳不要急着实现，等我批准才能动」
- 涉及文件/产物: `TASK.md` [修改-第 1 节待办]，`backend/src/main/resources/application.yml` [待创建-方案落地时]
- 已锁定决策: 批准前不动代码；方案候选=Redis/DB 分配 worker-id 或配置化；缓存候选=Redis 分布式缓存或接受 TTL 上界
- 阻塞点: 用户批准
- **下一步具体动作**: 用户批准后按 TASK.md 接入清单立项（先出方案给用户审，再实现）
- 验证方式: 方案获批；实现后双实例联调主键无冲突

### 2.3 M-4 异步化（待管理端界面设计时一并定，已给结论不建议现在做）

- 目标: 分片内容更新/批量标注不再阻塞 HTTP 请求线程（如需）
- 当前状态: 已核对并答复用户——**不建议现在做**（批量化已解决主要痛点；异步化需新增分片级向量状态字段+轮询接口，属契约设计）；管理端界面未设计
- 涉及文件/产物: `service/impl/DocumentChunkServiceImpl.java` [修改-已做批量化，未做异步化]
- 已锁定决策: 不引入分片级状态字段；等管理端界面设计时一起定
- **下一步具体动作**: 管理端界面设计启动时，把「分片向量重建状态 + 轮询接口」纳入契约设计一并评审
- 验证方式: 契约文档增补该接口设计

### 2.4 前端（frontend/ 与 student-frontend/ 均为空目录，未设计）

- 目标: 管理端 + C 端前端实现（契约基准已定稿）
- 当前状态: 用户 2026-08-16 指示「前端还没有设计，需要等后端写的差不多才写」；后端契约基准 = `docs/contracts/2026-08-16-接口契约定稿.md` [新建-已提交]
- 已锁定决策: 错误响应体统一 ApiResponse{code,message,data}（code=HTTP 状态码，409 并发 Run/503 文案见契约文档）；集合字段恒 []；可空对象字段恒 null 不缺 key；token_count 0=未统计
- 阻塞点: 后端剩余工作 + 用户启动信号
- **下一步具体动作**: 待用户指示；AGENTS.md 强制「前端 AI 对话/消息渲染/会话界面落地前必须与用户沟通」
- 验证方式: 前端按契约文档实现后联调

### 2.5 其余已答复的核对项（无需动作，仅记录结论防重复审查）

- L-1（disableUser 批量黑名单）/L-6（反馈 upsert 单 SQL）/L-10（空结果不缓存）/L-14（多实例缓存）/M-7 阶段超时：已答复用户——收益低或需专项设计，维持不动；详见记忆 bug-perf-fix-wave-2026-08-16-night
- 历史遗留观察项（上份交接文档 §2.2）：controller/dto/ 未归位 15 文件、IChatSessionService.findActiveSessions 无调用方、AdminUserControllerSecurityTest @MockBean 残留、IntegrationTestBase assertStatus/awaitStatus 零引用等——非本会话范围，未处理

## 3. 环境与依赖状态 (Environment)

- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17）、Maven 3.9.16（D:\code\envs\maven\3.9.16，settings.xml 指定本地仓库 D:/code/envs/maven/3.9.16/repo）；Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + MapStruct 1.6.3 + Testcontainers（postgresql/redis）；pre-commit（Python 3.14.6）
- **Docker 需手动启动**：集成测试（Auth/ChatFlow/Security 集成 + 5 个 mapper XML 测试 + 冒烟）用 Testcontainers 单例容器，Docker Desktop 未启动则这些用例失败；开发联调另需 `docker compose -f docker-compose.dev.yml up -d`
- 关键配置: `backend/src/test/resources/application-test.yml`（集成测试专用，dummy dashscope key）；`backend/.env` 只含 DASHSCOPE_API_KEY（gitignore）；新配置 `auth.strict-secret`（默认 false）、`etl.embedding-batch-size: 16`、`worker.run-pool.stale-run-timeout-minutes: 10`
- 安装/构建/启动命令:
  ```bash
  cd backend && mvn.cmd clean verify        # 全门禁（spotless+checkstyle+spotbugs+jacoco BUNDLE 0.80+单类 0.80；当前全绿 736）
  cd backend && mvn.cmd spotless:apply      # 格式化（Windows 会转 CRLF；改代码后先 apply 再 verify）
  cd backend && mvn.cmd test                # 仅测试（736 用例）
  ```
- 常用实证/坑: ①spotless:apply 在 Windows 把改过的文件转 CRLF，python 脚本改文件需按 \r\n 匹配（bash heredoc 用 PYEOF 分隔避免与内容冲突）；②Spring AI `embeddingModel.embed(List<String>)` 返回 `List<float[]>`（非 List<Embedding>）；③Caffeine `Cache.get(key, fn)` 返回 null 不记录映射（L-9 依赖此语义）；④jacoco 单类门禁对嵌套类/新类敏感——新类必须 ≥80% 行覆盖，record 参数类放 `record/` 包（已豁免）；⑤`doNothing()` 仅限 void 方法（Milvus delete 返回对象要用 doReturn(null)）

## 4. 全局约定 (Locked Conventions)

- 继承前两份交接文档全部约定（分层依赖/对象通信/MapStruct/Service 接口化/BizException/config-properties-record-constants 收编/主表 this.lambdaQuery()/按需取列/禁手动 IdWorker/缓存先写 DB 后失效/private final+@RequiredArgsConstructor/禁循环依赖/禁用弃用 API/注释日志全中文/查询必带分页/jacoco 单类 ≥80% 豁免 8 项/Testcontainers 单例容器模式）
- **新增——接口契约**（docs/contracts/2026-08-16-接口契约定稿.md，前端实现基准）：错误响应体统一 `ApiResponse{code,message,data}`，code 与 HTTP 状态码同值（409=并发 Run「会话 X 已有活跃的 Run，无法创建新 Run」、503=DB 不可用/消息队列不可用/文档解析队列繁忙）；集合字段恒输出 `[]` 不输出 null；可空对象字段恒输出 null 字段不缺 key；chat_message.token_count 语义 0=未统计
- **新增——BUG-7 部署约定**：生产必须设 `auth.strict-secret: true` 并配置 JWT_SECRET；开发保持 false（内置默认密钥仅限开发）
- **新增——BUG-8 待批准**：多实例部署事项（TASK.md 第 1 节）未经用户批准不得实现
- **新增——H-1 投递模型**：SSE 事件投递走每 run 有界队列+独立投递线程（推送与生成解耦）；回放批次锁内入队保证顺序；队列满摘除订阅者依赖 EventSource 自动重连+ring 回放补偿——后续改动不得把 send 移回生成线程
- git 纪律（只 add 任务文件，禁 git add -A）；docs/ 下审查报告等输入物文件不纳入提交；superpowers 流程固定（核验→spec 审批→plan 审批→SDD）；MapStruct/Lombok 配置变更后必须 `mvn clean` 重编译
