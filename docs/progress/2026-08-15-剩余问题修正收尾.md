# 进度文档 (Session Handoff)

> 本文件由 `session-handoff` 技能生成，是跨会话续接任务进度的**唯一事实来源**。
> 新会话在续接时，必须以本文件为准，不要凭空推测已完成的进度或重复已完成的工作。
> 本会话上下文：上份交接文档 `docs/progress/2026-08-15-宪法合规与主任务收尾.md`（§2.2 观察项 3 项 + 原观察①-⑤ + §2.3 Deferred minors）。

## 元信息 (Meta)
- workspace_dir: D:\code\project\commerce-customer\commerce-customer
- saved_at: 2026-08-15T22:30:00+08:00
- 版本控制提交号: a09d55f
- 任务一句话描述: **剩余问题全部修正（3 提交 e00a84c..a09d55f）——controller 出参 Entity/手写转换/观察①-⑤/deferred 六项清零，verify 全门禁绿（527 测试、jacoco 81.0%）**

## 1. 已完成 (Completed) — 简要标记

### 1.1 §2.2 观察项 3 项（commit e00a84c）
- [x] **AdminLoginRecordController VO 化**：K1/K2/K4 出参改 SysLoginRecordVO/SysTokenBlacklistVO（controller/vo，剔除 deleted）+ AdminLoginRecordConverter（service 包）
- [x] **AdminSessionController 手写 Map → MapStruct**：H1/H2 出参改 ChatSessionVO/ChatMessageVO/ChatSessionDetailVO + ChatSessionConverter（toSummaryVO/toMessageVO/toDetailVO 三方法）
- [x] **AdminEnrollmentController G4 转换下沉**：EnrollmentService 新增 findStudentCoursesAsDTO（内部 courseService.toDTO，无循环依赖）；controller 不再持有 CourseService/entity

### 1.2 原观察①-⑤（同一波浪清零，e00a84c + f04e6f9 + a09d55f）
- [x] **① StudentController 出 Entity**：实为 6 处手写 Map 转换 → StudentConverter（toCourseVO/toChunkVO/toChunkBriefVO/toChunkContextVO 多源含 null 关联/toSessionVO）+ 5 个 VO（StudentCourseVO/ChunkVO/ChunkBriefVO/ChunkContextVO/SessionVO）；顺带移除未使用 courseService 字段
- [x] **② AuthController 直触实体**：SysUserService.findByUsername → findAuthViewByUsername 返回 AuthUserView（service 包 record，含 passwordHash 仅供校验，不出边界）；旧方法删除
- [x] **③ service 反向依赖 controller/dto 包**：10 个 DTO（CourseDTO/StudentDTO/ScheduleDTO/UserDTO + 6 个 Create/Update Request）git mv 至根 `dto/` 包（AGENTS.md 正规位置）；24 文件 import 同步；2 个测试通配符 import 双保留
- [x] **④ FeedbackController.create 返回实体**：出参改 UserFeedbackVO（复用 UserFeedbackConverter）
- [x] **⑤ Db 静态工具**：CourseQueryService 4 处 Db.lambdaQuery/Db.getById → 注入 CourseInfoMapper/CourseContentMapper/CourseScheduleMapper + Wrappers 链式；**附带收益：低覆盖类 20% 的 mockStatic 障碍消失，补 7 个 DB 路径测试**

### 1.3 §2.3 Deferred minors（f04e6f9）
- [x] **CacheConfigTest TTL 断言**：Caffeine Policy API 实锤（FixedExpiration.getExpiresAfter/Eviction.getMaximum），断言 5min/512 与 60s/32 精确值
- [x] **CourseConverter 接口常量注入**：**实证排除**——MapStruct 1.6.3 生成 Impl 只调用无参构造器，抽象类带参构造器编译失败（已实验验证），回退接口常量并注释记录
- [x] **parseTags null/blank 直测**：CourseConverterTest 补 3 分支断言
- [x] **DeviceKickServiceTest setUp 注释收敛**：删除"降级方法已由 mapper mock 直测"过强表述
- [x] **EtlPipeline 失效精确化**：updateDocStatus 改状态白名单失效（CHUNKED/INDEXED/FAILED 影响统计口径；PARSING/PARSED/CHUNKING/EMBEDDING 中间态跳过）——统计口径已源码核实（pendingChunkCount 按 correctionStatus，CHUNKED 变更）
- [ ] **缓存共享可变实例**：维持现状（审查原判断"消费者只读，风险可控"；写侧副本化需 Page 重建，收益低风险引入）

### 1.4 新增测试与覆盖
- [x] 3 个 MapStruct Converter 真实实现测试（AdminLoginRecordConverterTest/ChatSessionConverterTest/StudentConverterTest）——controller 测试用 mock 导致新 Impl 0 覆盖，补测后 jacoco 从 76% 回 81.0%
- [x] EnrollmentServiceTest 补 findStudentCoursesAsDTO 2 测；SysUserServiceTest 补 findAuthViewByUsername 2 测；CourseQueryServiceTest 重写补 DB 路径 7 测
- [x] **verify 全门禁绿**（实测 2026-08-15 晚）：`mvn verify` BUILD SUCCESS——spotless ✓ checkstyle 0 violations ✓ spotbugs ✓ **jacoco 81.0% ≥ 0.80 ✓**（covered=3961/4893）；全量测试 527 个全绿（480 → 527，新增 47）

## 2. 进行中 / 未完成 (Pending) — 重点详述

### 2.1 已清零验证（2026-08-15 晚实测）
- [x] controller 出参含 Entity：grep 归零
- [x] service 依赖 controller.dto 包：grep 归零
- [x] Db 静态工具（extension.toolkit.Db）：grep 归零
- [x] 全路径类名：grep 归零

### 2.2 现状观察（非阻塞，供后续波浪参考）
- controller 内部持有 entity 变量（service 返回 entity → controller 转 VO）：AdminCourseController/AdminScheduleController/ChatController/StudentController 仍 import entity——出参合规（先例：P1-6 审查仅约束"直出"），如需彻底"Entity 不出 service 边界"需 service 层全面 VO 化（大波浪）
- service 依赖 controller.vo：11 个文件（转换器目标类型，8 个先例被审查接受）
- CourseQueryService 缓存返回共享可变实例（消费者只读，风险可控，deferred 维持）

### 2.3 遗留低覆盖类（jacoco 达标后非阻塞）
- DeviceKickService 44%、EtlPipeline 37%、CustomSummarizationHook 35%、CourseQueryService 20%（本次已从 0% 大幅改善）、ChatController 69%（reconnect/replayFromPg 需 SAA 流 mock）

## 3. 环境与依赖状态 (Environment)
- 运行时/工具版本: JDK 17（D:\code\java\jdk\jdk17）、Maven 3.9.16（D:\code\envs\maven\3.9.16，仓库 D:\code\envs\maven\3.9.16\repo）；Spring Boot 3.5.8 + Spring AI 1.1.2 + SAA 1.1.2.0 + MyBatis-Plus 3.5.12 + MapStruct 1.6.3 + Caffeine（Boot BOM）
- 关键配置: `backend/.env` 只含 DASHSCOPE_API_KEY（gitignore）；PG/Redis/MinIO 凭证与 docker-compose.dev.yml 一致
- 安装/构建/启动命令:
  ```bash
  docker compose -f docker-compose.dev.yml up -d   # 基础设施
  cd backend && mvn.cmd test                        # 全量测试（当前 527/527）
  cd backend && mvn.cmd verify                      # 全门禁（spotless+checkstyle+spotbugs+jacoco；**当前全绿 81.0%**）
  cd backend && mvn.cmd spotless:apply              # 格式化（提交前自证）
  ```

## 4. 全局约定 (Locked Conventions)
- AGENTS.md 为纯工程宪法：分层依赖（controller→service→mapper）/ Entity 不出 service 边界 / **DTO 在根 dto/ 包、VO 在 controller/vo 包**（本次移包后已对齐）/ 层间转换必须 MapStruct 禁手写（转换器与使用 Service 同包命名 XxxConverter）/ Wrapper 一律 Wrappers 链式禁 new / 禁业务层拼 SQL（复杂 SQL 走 mapper XML）/ 缓存先写 DB 后失效 / 注释日志全中文 / 查询按需字段 + 分页
- **MapStruct 改造后必须 `mvn.cmd clean` 重编译**（增量编译不重新生成实现类）；**MapStruct Impl 只调无参构造器**（抽象类构造注入不可行，转换器依赖经接口常量或注入式 uses 解决）
- **checkstyle 只查 src 源码**（pom includes 已限定；target/generated-sources 的 MapStruct Impl 不检查）
- **Git Bash 批处理坑**：grep -rln 输出反斜杠路径 + CRLF 行尾，for 循环前必须 `tr '\\' '/' | tr -d '\r'`
- superpowers 流程固定：核验（源码实锤）→ spec 审批 → plan 审批 → SDD；用户决策必须源码/官方文档实锤；重大交付物需用户审批
- git 纪律：`git add` 只加任务文件禁 `git add -A`；提交 `--no-verify`（pre-commit hook 历史遗留阻塞）
- 开发库无业务数据可 drop 重建；V6/V7 迁移直接改 + drop 重建，不加增量迁移
- 前端（frontend/ 与 student-frontend/）为空目录未实现；前端对话界面落地前必须与用户沟通
